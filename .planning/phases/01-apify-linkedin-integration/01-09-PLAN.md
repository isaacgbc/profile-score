---
phase: 01-apify-linkedin-integration
plan: 09
type: execute
wave: 5
depends_on:
  - "01-07"
  - "01-08"
files_modified:
  - src/components/input/LinkedinUrlPrimaryInput.tsx
  - src/app/input/page.tsx
  - src/components/ui/GenerationProgress.tsx
  - src/hooks/useLinkedinScrape.ts
autonomous: false
requirements:
  - REQ-01
  - REQ-03
must_haves:
  truths:
    - "Primary input on /input is a LinkedIn URL field with inline validation against linkedin.com/in/..."
    - "CV upload is demoted to a secondary <details> toggle but remains fully functional"
    - "User can submit a URL and see a loading state with 'Analizando tu perfil de LinkedIn...' (ES) or 'Analyzing your LinkedIn profile...' (EN)"
    - "GenerationProgress.tsx includes a 'scraping_profile' step that renders BEFORE 'parsing'"
    - "All 4 localized error states (private, invalid URL, rate limit, downtime) render with recovery actions (retry, try CV, upgrade)"
    - "useLinkedinScrape hook handles POST /api/scrape-linkedin, maps error codes to i18n keys, updates AppContext"
    - "Styling uses only CSS variables (var(--accent), var(--surface-*), var(--text-*), var(--border-*)) — zero raw Tailwind color classes"
  artifacts:
    - path: "src/components/input/LinkedinUrlPrimaryInput.tsx"
      provides: "URL input field + inline validation + submit handler wiring"
    - path: "src/app/input/page.tsx"
      provides: "Restructured primary URL input + CV upload demoted to <details>"
    - path: "src/components/ui/GenerationProgress.tsx"
      provides: "scraping_profile step added to stageMap"
    - path: "src/hooks/useLinkedinScrape.ts"
      provides: "triggerScrape(url) + error-code → i18n mapping"
      exports: ["useLinkedinScrape"]
  key_links:
    - from: "src/app/input/page.tsx"
      to: "src/components/input/LinkedinUrlPrimaryInput.tsx"
      via: "component import + onSubmit handler"
      pattern: "LinkedinUrlPrimaryInput"
    - from: "src/hooks/useLinkedinScrape.ts"
      to: "/api/scrape-linkedin"
      via: "fetch POST"
      pattern: "/api/scrape-linkedin"
    - from: "src/components/ui/GenerationProgress.tsx"
      to: "ProgressStage type"
      via: "stageMap lookup"
      pattern: "scraping_profile"
---

<objective>
Redesign the `/input` page so LinkedIn URL is the primary input, add a URL-first component with inline validation, add a `scraping_profile` step to the generation progress UI, and build a client hook (`useLinkedinScrape`) that drives POST `/api/scrape-linkedin` and maps error codes to the 4 localized recovery actions. The CV upload path must remain fully functional as a secondary toggle (demoted, not deleted).

Purpose: This is the first Wave 3 UI plan — it builds all the INPUT-side user-facing pieces for the Apify flow. Plan 01-10 will handle the state management (AppContext) and the results-side dashboard chip.

Output: 4 files touched — new component, updated page, updated progress component, new hook. Uses CSS variables only. EN+ES parity via existing i18n keys from Plan 01-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-apify-linkedin-integration/01-CONTEXT.md
@.planning/phases/01-apify-linkedin-integration/01-RESEARCH.md
@CLAUDE.md
@.claude/rules/components.md
@.claude/skills/create-component
@src/app/input/page.tsx
@src/components/ui/GenerationProgress.tsx
@src/lib/i18n/en.json
@src/lib/i18n/es.json
@src/context/AppContext.tsx
@src/context/I18nContext.tsx
@src/lib/schemas/linkedin-profile.ts
@src/lib/types/index.ts

<interfaces>
From Plan 01-01 i18n keys (already present in en.json + es.json):
- `input.linkedinUrlPrimaryLabel`, `input.linkedinUrlHelp`, `input.linkedinUrlPlaceholder`, `input.useCvAlternativeTitle`
- `progress.stageScraping`
- `apify.error.private`, `apify.error.invalidUrl`, `apify.error.rateLimit`, `apify.error.downtime`, `apify.error.quotaExceeded`, `apify.error.generic`, `apify.error.disabled`
- `apify.actions.retry`, `apify.actions.switchToCv`, `apify.actions.upgrade`

From Plan 01-05 types:
```
export type ApifyScrapeError = "APIFY_INVALID_URL" | "APIFY_PROFILE_PRIVATE" | "APIFY_RATE_LIMITED" | "APIFY_CIRCUIT_OPEN" | "APIFY_QUOTA_EXCEEDED" | "APIFY_SCRAPE_FAILED" | "APIFY_DISABLED" | "APIFY_TOKEN_MISSING";
```

From Plan 01-07 route response:
```
POST /api/scrape-linkedin
Success: { profile: HarvestProfile, cached: boolean, quotaRemaining: number|null, plan: "free"|"starter"|"recommended", source: "apify" }
Error: { error: string, retryAfter?: number }
Status codes: 400, 402, 422, 429, 500, 503
```

From Plan 01-08 orchestrator:
- `ProgressStage` union now includes `"scraping_profile"`

From existing src/context/I18nContext.tsx (skim to confirm):
- Exports `useT()` or `useTranslations()` hook that returns a `t(key: string)` function
- Supports interpolation for `{count}` placeholder

From existing src/components/ui/GenerationProgress.tsx stageMap region (per research line 59-68):
- stageMap: Record<ProgressStage, string> — executor reads current structure

From CLAUDE.md + .claude/rules/components.md:
- `"use client"` required for interactivity (input, submit handler, validation)
- CSS variables ONLY: `var(--accent)`, `var(--surface-*)`, `var(--text-*)`, `var(--border-*)`. NO `bg-blue-500`, `text-gray-900`, etc.

From src/lib/schemas/linkedin-profile.ts:
- `scrapeLinkedinRequestSchema.safeParse({url})` for inline client-side validation — reuse the same Zod schema
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useLinkedinScrape hook + LinkedinUrlPrimaryInput component</name>
  <files>src/hooks/useLinkedinScrape.ts, src/components/input/LinkedinUrlPrimaryInput.tsx</files>
  <read_first>
    - /Users/isaacgbc/profile-score/src/hooks (list existing hooks to match style: useGenerationStream, useProgressPolling, useExport)
    - /Users/isaacgbc/profile-score/src/hooks/useGenerationStream.ts (skim — fetch pattern, error handling)
    - /Users/isaacgbc/profile-score/src/context/I18nContext.tsx (confirm useT hook export name)
    - /Users/isaacgbc/profile-score/src/lib/schemas/linkedin-profile.ts (reuse scrapeLinkedinRequestSchema for client-side validation)
    - /Users/isaacgbc/profile-score/src/components/input (list existing components — match naming + prop style)
  </read_first>
  <action>
    Step 1. Create `src/hooks/useLinkedinScrape.ts`:
    ```
    "use client";
    import { useState, useCallback } from "react";
    import { scrapeLinkedinRequestSchema } from "@/lib/schemas/linkedin-profile";
    import type { HarvestProfile, ApifyScrapeError, ApifyQuotaState } from "@/lib/types";

    export type ScrapeStatus = "idle" | "scraping" | "done" | "error";

    const ERROR_CODE_TO_I18N: Record<string, string> = {
      APIFY_INVALID_URL: "apify.error.invalidUrl",
      APIFY_PROFILE_PRIVATE: "apify.error.private",
      APIFY_RATE_LIMITED: "apify.error.rateLimit",
      APIFY_CIRCUIT_OPEN: "apify.error.downtime",
      APIFY_DOWNTIME: "apify.error.downtime",
      APIFY_QUOTA_EXCEEDED: "apify.error.quotaExceeded",
      APIFY_DISABLED: "apify.error.disabled",
      APIFY_SCRAPE_FAILED: "apify.error.generic",
    };

    export function useLinkedinScrape() {
      const [status, setStatus] = useState<ScrapeStatus>("idle");
      const [profile, setProfile] = useState<HarvestProfile | null>(null);
      const [errorKey, setErrorKey] = useState<string | null>(null);
      const [errorCode, setErrorCode] = useState<string | null>(null);
      const [quotaState, setQuotaState] = useState<ApifyQuotaState | null>(null);

      const triggerScrape = useCallback(async (rawUrl: string) => {
        setStatus("scraping");
        setErrorKey(null);
        setErrorCode(null);
        setProfile(null);

        // Client-side Zod pre-check (cheap, avoids a round-trip)
        const parsed = scrapeLinkedinRequestSchema.safeParse({ url: rawUrl });
        if (!parsed.success) {
          setStatus("error");
          setErrorCode("APIFY_INVALID_URL");
          setErrorKey("apify.error.invalidUrl");
          return;
        }

        try {
          const res = await fetch("/api/scrape-linkedin", {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({ url: parsed.data.url }),
          });
          const body = await res.json().catch(() => ({}));

          if (!res.ok) {
            const code = typeof body?.error === "string" ? body.error : "APIFY_SCRAPE_FAILED";
            setStatus("error");
            setErrorCode(code);
            setErrorKey(ERROR_CODE_TO_I18N[code] ?? "apify.error.generic");
            return;
          }

          setProfile(body.profile as HarvestProfile);
          setQuotaState({ remaining: body.quotaRemaining ?? null, plan: body.plan ?? "free" });
          setStatus("done");
        } catch (err) {
          setStatus("error");
          setErrorCode("APIFY_SCRAPE_FAILED");
          setErrorKey("apify.error.generic");
        }
      }, []);

      const reset = useCallback(() => {
        setStatus("idle");
        setProfile(null);
        setErrorKey(null);
        setErrorCode(null);
      }, []);

      return { status, profile, errorKey, errorCode, quotaState, triggerScrape, reset };
    }
    ```

    Step 2. Create `src/components/input/LinkedinUrlPrimaryInput.tsx`:
    - `"use client"` directive at top
    - Props: `{ onSubmit: (url: string) => void | Promise<void>; isSubmitting: boolean; errorKey: string | null; onRetry?: () => void; onSwitchToCv?: () => void }`
    - Uses `useT()` from I18nContext
    - Renders:
      - `<label>` with `t("input.linkedinUrlPrimaryLabel")`
      - `<input type="url" inputMode="url">` with placeholder `t("input.linkedinUrlPlaceholder")`
      - Helper text `t("input.linkedinUrlHelp")`
      - Inline validation: on blur, run `scrapeLinkedinRequestSchema.safeParse({url: value})` and show the i18n error if invalid
      - Submit button disabled when input empty, invalid, or `isSubmitting === true`
      - Error banner (when `errorKey` set) with:
        - `t(errorKey)` as message
        - Two action buttons: "Try again" (calls `onRetry`) + "Upload CV instead" (calls `onSwitchToCv`)
        - For `apify.error.quotaExceeded`, show "Upgrade" button linking to `/pricing`
    - Styling: Use Tailwind utility classes for layout/spacing (flex, gap, p-*, rounded-*, etc.) but NEVER color classes. Colors come from CSS variables inline:
      - `style={{ backgroundColor: "var(--surface-1)", color: "var(--text-primary)", borderColor: "var(--border-subtle)" }}` on container
      - `style={{ backgroundColor: "var(--accent)", color: "var(--accent-foreground)" }}` on primary button
      - Or use Tailwind arbitrary values: `className="bg-[var(--surface-1)] text-[var(--text-primary)] border-[var(--border-subtle)]"`
    - Do NOT import `bg-blue-500`, `text-gray-900`, `border-gray-200`, or any raw Tailwind color. Ever.

    Step 3. Neither file should import anything from Plan 01-10 (AppContext additions). This plan is self-contained.
  </action>
  <verify>
    <automated>grep -q "export function useLinkedinScrape" src/hooks/useLinkedinScrape.ts && grep -q "/api/scrape-linkedin" src/hooks/useLinkedinScrape.ts && grep -q "apify.error" src/hooks/useLinkedinScrape.ts && grep -q "LinkedinUrlPrimaryInput" src/components/input/LinkedinUrlPrimaryInput.tsx && grep -q "\"use client\"" src/components/input/LinkedinUrlPrimaryInput.tsx && grep -q "var(--" src/components/input/LinkedinUrlPrimaryInput.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File `src/hooks/useLinkedinScrape.ts` exists and exports `useLinkedinScrape`
    - `grep -q "/api/scrape-linkedin" src/hooks/useLinkedinScrape.ts` succeeds
    - `grep -q "APIFY_INVALID_URL" src/hooks/useLinkedinScrape.ts` succeeds (all 8 error codes mapped)
    - `grep -q "APIFY_PROFILE_PRIVATE" src/hooks/useLinkedinScrape.ts` succeeds
    - `grep -q "APIFY_QUOTA_EXCEEDED" src/hooks/useLinkedinScrape.ts` succeeds
    - `grep -q "APIFY_CIRCUIT_OPEN\\|APIFY_DOWNTIME" src/hooks/useLinkedinScrape.ts` succeeds
    - `grep -q "scrapeLinkedinRequestSchema" src/hooks/useLinkedinScrape.ts` succeeds (client-side validation)
    - File `src/components/input/LinkedinUrlPrimaryInput.tsx` exists
    - `grep -q "\"use client\"" src/components/input/LinkedinUrlPrimaryInput.tsx` succeeds
    - `grep -q "var(--" src/components/input/LinkedinUrlPrimaryInput.tsx` succeeds (CSS variables used)
    - `grep -E "bg-(red|blue|green|yellow|gray|slate|zinc|neutral|stone)-[0-9]" src/components/input/LinkedinUrlPrimaryInput.tsx` returns 0 matches (no raw Tailwind colors)
    - `grep -E "text-(red|blue|green|yellow|gray|slate)-[0-9]" src/components/input/LinkedinUrlPrimaryInput.tsx` returns 0 matches
    - `grep -q "input.linkedinUrlPrimaryLabel" src/components/input/LinkedinUrlPrimaryInput.tsx` succeeds (i18n key used)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Hook + component built, 8 error codes mapped, zero raw Tailwind colors, build clean.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure /input page to make URL primary + CV secondary</name>
  <files>src/app/input/page.tsx</files>
  <read_first>
    - /Users/isaacgbc/profile-score/src/app/input/page.tsx (FULL — read entire file, understand the current LinkedinInputSection component usage and submit flow)
    - /Users/isaacgbc/profile-score/.planning/phases/01-apify-linkedin-integration/01-CONTEXT.md D-20
    - /Users/isaacgbc/profile-score/.planning/phases/01-apify-linkedin-integration/01-RESEARCH.md lines 701-705 (UX Changes Summary for input page)
  </read_first>
  <action>
    Step 1. Restructure `src/app/input/page.tsx`:
    - Import the new `LinkedinUrlPrimaryInput` component
    - Import the `useLinkedinScrape` hook
    - Hoist the existing CV/PDF upload UI into a `<details>` element with `<summary>{t("input.useCvAlternativeTitle")}</summary>`
    - The existing `LinkedinInputSection` (or whatever component currently handles paste/upload) moves inside the `<details>` as a fallback option
    - The primary UI flow is now:
      ```
      <LinkedinUrlPrimaryInput
        isSubmitting={scrapeStatus === "scraping"}
        errorKey={scrapeErrorKey}
        onSubmit={async (url) => {
          await triggerScrape(url);
          // if status becomes "done", navigate to /results (or the existing post-scrape flow)
        }}
        onRetry={() => { reset(); }}
        onSwitchToCv={() => { /* open the <details> toggle programmatically OR scroll to it */ }}
      />
      <details>
        <summary>{t("input.useCvAlternativeTitle")}</summary>
        {/* Existing LinkedinInputSection / CvUploadSection here — UNCHANGED */}
      </details>
      ```
    - Wire the `useLinkedinScrape` hook to trigger `/api/scrape-linkedin`. When the scrape succeeds, pass the returned profile through to the audit generate flow. (For now, store profile in local state OR via existing context — Plan 01-10 will formalize context state.)
    - When the scrape succeeds: call the existing audit-generate flow (POST to `/api/audit/generate`) passing `preparsedLinkedinSections` (built from formatter) and `linkedinProfileSource: "apify"`. If the formatter needs to run client-side, import `profileToSectionRecord` from `@/lib/services/linkedin-profile-formatter`. (Note: the formatter is pure TS with no server dependencies — safe to import in client component as long as no `@/lib/db/client` transitive import exists. If it does, move the formatter call to a separate /api/linkedin/prepare endpoint — but based on Plan 01-05 the formatter has no DB imports, so client-side is fine.)
    - Do NOT delete any existing component. Do NOT remove the ability to use CV upload or LinkedIn paste. All 3 input modes (URL scrape, CV upload, LinkedIn paste) must remain functional.
    - Preserve `"use client"` directive if present; it IS currently a client component.

    Step 2. Error handling: when `scrapeStatus === "error"`, the `LinkedinUrlPrimaryInput` component shows the error banner (wired via `errorKey` prop). The page-level wiring just passes through.

    Step 3. Loading state: when `scrapeStatus === "scraping"`, show the existing `GenerationProgress` component with `currentStage = "scraping_profile"`.

    Step 4. Do NOT change the existing audit-generate API call shape beyond adding `preparsedLinkedinSections` and `linkedinProfileSource` fields. These are the fields Plan 01-08 added to the shared schema — confirm by grep before the change.
  </action>
  <verify>
    <automated>grep -q "LinkedinUrlPrimaryInput" src/app/input/page.tsx && grep -q "useLinkedinScrape" src/app/input/page.tsx && grep -q "<details>" src/app/input/page.tsx && grep -q "useCvAlternativeTitle" src/app/input/page.tsx && grep -q "preparsedLinkedinSections\\|linkedinProfileSource" src/app/input/page.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "LinkedinUrlPrimaryInput" src/app/input/page.tsx` succeeds
    - `grep -q "useLinkedinScrape" src/app/input/page.tsx` succeeds
    - `grep -q "<details>" src/app/input/page.tsx` succeeds (CV upload demoted)
    - `grep -q "useCvAlternativeTitle" src/app/input/page.tsx` succeeds
    - `grep -q "preparsedLinkedinSections\\|linkedinProfileSource" src/app/input/page.tsx` succeeds (apify path wired to audit generate)
    - `grep -q "profileToSectionRecord\\|profileToMarkdown" src/app/input/page.tsx` succeeds (formatter imported OR called via helper)
    - Previous LinkedIn input section component reference STILL exists in the file (existing paste/upload paths preserved inside `<details>`)
    - `npm run build` exits 0
    - Pre-existing CV upload handler is still wired (a grep for the CV upload handler function name still finds it)
  </acceptance_criteria>
  <done>Input page restructured, URL primary, CV secondary, all 3 input paths functional, build clean.</done>
</task>

<task type="auto">
  <name>Task 3: Add scraping_profile step to GenerationProgress stageMap</name>
  <files>src/components/ui/GenerationProgress.tsx</files>
  <read_first>
    - /Users/isaacgbc/profile-score/src/components/ui/GenerationProgress.tsx (FULL — find stageMap region at lines 59-68 per research)
    - /Users/isaacgbc/profile-score/.planning/phases/01-apify-linkedin-integration/01-RESEARCH.md lines 717-722
  </read_first>
  <action>
    Step 1. Find the `stageMap` (or equivalent `Record<ProgressStage, string>`) in `src/components/ui/GenerationProgress.tsx`. It maps each stage enum value to a user-facing label (likely via i18n `t()` call).

    Step 2. Add a new entry:
    ```
    scraping_profile: progressStrings.stageScraping ?? t("progress.stageScraping") ?? "Analyzing your LinkedIn profile...",
    ```
    (Match whatever pattern the existing map uses — some projects read from a prebuilt `progressStrings` object, others call `t()` inline.)

    Step 3. Ensure the step appears FIRST in the visual ordering (before "parsing", "scoring", etc.). The progress component likely renders stages as a list or stepper — the executor must confirm the visual ordering logic and place `scraping_profile` as step 0.

    Step 4. If the component infers "completed" vs "current" vs "pending" based on the current stage index or stage order, update the ordering array to include `scraping_profile` at the start.

    Step 5. Do NOT change: existing stages, existing labels, the component's visual styling, the progress calculation (% complete). Additive only.

    Step 6. When `currentStage !== "scraping_profile"` and the flow started from CV upload, the scraping step is skipped visually (either shown as "skipped" or not rendered at all — match whatever convention the existing stages use for the CV flow's skipped steps).
  </action>
  <verify>
    <automated>grep -q "scraping_profile" src/components/ui/GenerationProgress.tsx && grep -q "stageScraping\\|progress.stageScraping" src/components/ui/GenerationProgress.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "scraping_profile" src/components/ui/GenerationProgress.tsx` succeeds
    - `grep -q "stageScraping" src/components/ui/GenerationProgress.tsx` succeeds (i18n key referenced)
    - `npm run build` exits 0 (TS union type from Plan 01-08 resolves)
    - Existing stage labels (queued, parsing, scoring, rewriting, ...) still present in the file — this is an additive change, nothing removed
  </acceptance_criteria>
  <done>Progress component recognizes scraping_profile as a valid stage with localized label.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: CHECKPOINT — Manual UI smoke test (EN + ES)</name>
  <files>src/app/input/page.tsx, src/components/input/LinkedinUrlPrimaryInput.tsx, src/components/ui/GenerationProgress.tsx</files>
  <action>CHECKPOINT — human visual verification. Isaac runs the dev server and manually confirms the 9 checks in how-to-verify below: primary URL input visible, CV upload preserved as details toggle, inline validation works, 4 error states render correctly (invalid URL, quota exceeded, private profile, downtime) in BOTH EN and ES, CV regression clean, no console errors, CSS variables resolve correctly, final npm run build exits 0.</action>
  <verify><automated>npm run build</automated></verify>
  <done>Isaac types "approved" after all 9 manual checks pass. Build gate green.</done>
  <what-built>Tasks 1-3 built the primary URL input, the useLinkedinScrape hook, restructured the input page, and added the scraping_profile progress step. Time to verify the flow visually in a real browser.</what-built>
  <how-to-verify>
    1. **Start dev server**: `npm run dev`
    2. **Visit** http://localhost:3000/input (or wherever the input page mounts). Confirm:
       - Primary UI is a LinkedIn URL input field (NOT paste/upload)
       - Placeholder shows `https://linkedin.com/in/yourname` (or the localized equivalent)
       - Helper text "We'll analyze your full profile in seconds" is visible
       - A `<details>` element labeled "Prefer to upload your CV instead?" appears BELOW the URL field
       - Clicking the details expands to reveal the existing CV upload UI (functional, nothing broken)
    3. **Enter an invalid URL** `https://google.com/in/foo` and blur:
       - Inline validation message appears ("That doesn't look like a LinkedIn profile URL...")
       - Submit button is disabled
    4. **Enter a valid URL** `https://linkedin.com/in/isaacgbc` (or any public profile you own):
       - Inline validation passes
       - Submit button enabled
       - Click Submit → progress indicator shows "Analyzing your LinkedIn profile..." FIRST, before other stages
       - Scrape completes OR fails with a specific error banner
    5. **Switch language to ES** (via whatever language toggle exists, or visit `/input?lang=es`):
       - Label becomes "Pega la URL de tu perfil de LinkedIn"
       - Placeholder becomes "https://linkedin.com/in/tunombre"
       - Helper text: "Analizaremos tu perfil completo en segundos"
       - Details toggle: "¿Prefieres subir tu CV?"
       - Scrape loading copy: "Analizando tu perfil de LinkedIn..."
    6. **Force each error state** (requires setting up test conditions):
       - **Invalid URL**: enter `https://google.com/in/foo` — verify `apify.error.invalidUrl` copy displays
       - **Quota exceeded**: after 1 successful scrape as anon, try a second different URL — should show `apify.error.quotaExceeded` with "Upgrade plan" CTA
       - **Private profile**: enter a known-private profile URL — should show `apify.error.private` with "Upload CV instead" CTA
       - **Downtime**: `APIFY_ENABLED=false npm run dev` then try any URL — should show `apify.error.disabled`
       - Each error banner must show EN + ES versions correctly (toggle language)
    7. **Verify CV flow still works**: open the `<details>`, upload a sample CV PDF, confirm the audit runs end-to-end (this is the regression check for REQ-03 — CV upload preserved).
    8. **Console check**: open browser devtools, confirm NO console errors about missing i18n keys (watch for literal `input.linkedinUrlPrimaryLabel` rendering — means key wasn't looked up).
    9. **CSS variable check**: open devtools Styles pane, click on any new element, confirm colors resolve to `var(--accent)`, `var(--surface-*)`, `var(--text-*)` — NOT `rgb(59, 130, 246)` (which would indicate `bg-blue-500`).

    Build gate: `npm run build` exits 0
  </how-to-verify>
  <resume-signal>Type "approved" once all 9 manual checks pass. Or type "blocked: <which check failed>" with a screenshot/description.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client input -> hook | Raw URL string; Zod pre-validates client-side (UX) AND server-side (security) |
| Hook -> /api/scrape-linkedin | Trusted server route; hook forwards URL |
| Server response -> hook state | Route returns typed error codes; hook maps to i18n keys |
| UI -> scraped profile | Profile data rendered via React JSX (auto-escaped) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 (T-09: SSRF via client-side bypass) | Information Disclosure | Client-side validation | accept as UX | Client-side Zod is UX only. Security enforcement is at `/api/scrape-linkedin` route (Plan 01-07). Attacker bypassing client Zod still hits server Zod. |
| T-09-02 (XSS via rendered scraped profile) | Tampering | React JSX rendering | mitigate | React auto-escapes text content. Hook never calls `dangerouslySetInnerHTML` on scraped fields. Plan 01-10 results page inherits this protection. |
| T-09-03 (T-07: error code injection from response body) | Tampering | `useLinkedinScrape` error mapping | mitigate | `ERROR_CODE_TO_I18N` is a static lookup; unknown codes fall through to `apify.error.generic`. Hook never eval's response body or interpolates it into a DOM string. |
| T-09-04 (T-02: PII exposure via browser console logs) | Information Disclosure | Client state | mitigate | Hook stores profile in React state only — never calls `console.log(profile)`. Route response contains profile (intentional for audit flow), but DevTools access requires physical access to the user's machine — not in threat model. |
| T-09-05 (Quota state leak via client fetch) | Information Disclosure | `quotaState` display | accept | `quotaState.remaining` is intentionally surfaced to the dashboard chip (REQ-04). Not sensitive. |
| T-09-06 (CSRF on /api/scrape-linkedin from another origin) | Tampering | Route + hook | mitigate | Same-origin fetch inherits cookies; cross-origin POST requires preflight CORS (not configured, so browser blocks). Supabase auth session is httpOnly cookie — not accessible to foreign scripts. |
</threat_model>

<verification>
- [ ] `npm run build` exits 0
- [ ] All 4 UI files updated (input page, 2 new files, progress component)
- [ ] Zero raw Tailwind color classes in new components
- [ ] i18n keys resolve in both EN and ES
- [ ] Primary URL input validates client-side before submitting
- [ ] Loading state shows scraping step first
- [ ] All 4 error states render with localized copy + recovery actions
- [ ] CV upload flow remains functional (regression check for REQ-03)
- [ ] Manual smoke test checkpoint approved
</verification>

<success_criteria>
1. `/input` page has LinkedIn URL field as primary input
2. CV upload demoted to `<details>` toggle but fully functional
3. `useLinkedinScrape` hook maps all 8 error codes to correct i18n keys
4. `GenerationProgress` shows scraping step as first stage for Apify path
5. Component uses CSS variables ONLY (no raw Tailwind colors)
6. EN and ES both render correct copy
7. Build clean
</success_criteria>

<output>
After completion, create `.planning/phases/01-apify-linkedin-integration/01-09-SUMMARY.md` with:
- Screenshots or descriptions of each manual check
- Any CSS variable mapping issues encountered
- Confirmation that CV upload regression is clean
- Note any additional i18n keys that needed to be added beyond Plan 01-01's set
</output>