---
phase: 01-apify-linkedin-integration
plan: 07
type: execute
wave: 3
depends_on:
  - "01-03"
  - "01-04"
  - "01-05"
  - "01-06"
files_modified:
  - src/app/api/scrape-linkedin/route.ts
  - src/app/api/scrape-linkedin/__tests__/route.test.ts
  - src/app/api/webhooks/creala/route.ts
  - src/app/api/webhooks/creala/__tests__/route.test.ts
autonomous: true
requirements:
  - REQ-01
  - REQ-04
must_haves:
  truths:
    - "POST /api/scrape-linkedin accepts {url} and returns {profile, cached, quotaRemaining, source}"
    - "Route declares runtime='nodejs' and maxDuration=60 (needed for Apify sync call)"
    - "Route orchestrates: fingerprint → rate limit → url normalize → cache read → quota check → scrape → cache write → consume quota → log usage"
    - "Every error path returns a typed JSON error code matching i18n keys"
    - "APIFY_ENABLED=false kill switch short-circuits before any logic"
    - "Creala webhook updates apifyScrapeQuota=null on plan upgrade WITHOUT refactoring HMAC verification"
  artifacts:
    - path: "src/app/api/scrape-linkedin/route.ts"
      provides: "POST handler + maxDuration=60 + runtime=nodejs"
      exports: ["POST", "maxDuration", "runtime"]
    - path: "src/app/api/webhooks/creala/route.ts"
      provides: "Updated with apifyScrapeQuota: null on plan upgrade (1-line per case)"
      contains: "apifyScrapeQuota: null"
  key_links:
    - from: "src/app/api/scrape-linkedin/route.ts"
      to: "src/lib/services/apify-scraper-client.ts"
      via: "scrapeLinkedinProfile(...)"
      pattern: "scrapeLinkedinProfile"
    - from: "src/app/api/scrape-linkedin/route.ts"
      to: "src/lib/services/apify-quota.ts"
      via: "checkQuota + consumeQuota + logUsage"
      pattern: "checkQuota|consumeQuota|logUsage"
    - from: "src/app/api/scrape-linkedin/route.ts"
      to: "src/lib/services/apify-cache.ts"
      via: "normalizeLinkedinUrl + readCache + writeCache"
      pattern: "normalizeLinkedinUrl|readCache|writeCache"
    - from: "src/app/api/webhooks/creala/route.ts"
      to: "prisma.user.update"
      via: "one-line addition inside existing handler"
      pattern: "apifyScrapeQuota: null"
---

<objective>
Create the `/api/scrape-linkedin` POST route that orchestrates the full scrape flow: validate → rate-limit → fingerprint → normalize → cache-read → quota-check → Apify call → cache-write → quota-consume → usage-log → respond. Also add the single-line `apifyScrapeQuota: null` insertion to the existing Creala webhook at the already-verified plan-upgrade branch (D-14) — NO refactoring of HMAC verification or handler shape.

Purpose: This is the user-facing API endpoint. It's the ONLY place the full scrape pipeline runs end-to-end. Every error code the client handles originates here. The Creala webhook change is one line per case — trivial in isolation but critical for REQ-04 (paid tiers get unlimited).

Output: Working POST route + Creala webhook updated + route integration tests (mocked Apify + Prisma) + Creala webhook regression test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-apify-linkedin-integration/01-CONTEXT.md
@.planning/phases/01-apify-linkedin-integration/01-RESEARCH.md
@CLAUDE.md
@.claude/rules/api-routes.md
@.claude/rules/security.md
@src/app/api/audit/generate/route.ts
@src/app/api/audit/stream/route.ts
@src/app/api/webhooks/creala/route.ts
@src/lib/services/apify-scraper-client.ts
@src/lib/services/apify-cache.ts
@src/lib/services/apify-fingerprint.ts
@src/lib/services/apify-quota.ts
@src/lib/services/rate-limiter.ts
@src/lib/services/error-logger.ts
@src/lib/schemas/linkedin-profile.ts

<interfaces>
From Plan 01-04 scraper client:
```
export async function scrapeLinkedinProfile(params: { profileUrl: string; requestId: string }): Promise<{ runId: string; items: unknown[]; costUsd: number }>
```
Throws: APIFY_TOKEN_MISSING, APIFY_CIRCUIT_OPEN, APIFY_RUN_FAILED, APIFY_RUN_TIMED-OUT, APIFY_PROFILE_PRIVATE, APIFY_SCRAPE_FAILED

From Plan 01-03 cache:
```
normalizeLinkedinUrl(raw: string): string // throws APIFY_INVALID_URL
hashUrl(url: string): string
readCache(normalizedUrl): Promise<unknown | null>
writeCache({normalizedUrl, profileData, apifyRunId, costUsd}): Promise<void>
```

From Plan 01-03 fingerprint:
```
computeFingerprint(ip: string, userAgent?: string | null): string
```

From Plan 01-06 quota:
```
checkQuota({userId, fingerprintHash, isCacheHit}): Promise<void> // throws APIFY_QUOTA_EXCEEDED
consumeQuota({userId, fingerprintHash}): Promise<void>
getQuotaState({userId, fingerprintHash}): Promise<{remaining, plan}>
logUsage({userId, fingerprintHash, url, urlHash, cacheHit, apifyRunId, costUsd, success, errorCode, durationMs}): Promise<void>
```

From Plan 01-04 rate limiter:
```
export const scrapeRateLimiter; // 3 req/min per key
scrapeRateLimiter.check(key): { allowed: boolean; retryAfter?: number }
```

From Plan 01-01 schema:
```
scrapeLinkedinRequestSchema: z.object({ url: z.string().refine(...) })
harvestProfileSchema: z.object({...}).passthrough()
```

From existing src/app/api/audit/stream/route.ts (pattern for maxDuration):
```
export const runtime = "nodejs";
export const maxDuration = 120;
```

From existing src/app/api/webhooks/creala/route.ts (VERIFIED lines 238-272 per research):
- Handles `new_sale` + `new_subscription` cases
- Inside each case block: existing `prisma.user.update({ where: { ... }, data: { activePlanId, subscriptionStatus, subscriptionExpiresAt } })`
- Plan 01-07 adds ONE line to each `data` object: `apifyScrapeQuota: null`
- NO changes to HMAC verifySignature, timing-safe equality, handler signature

From src/lib/services/error-logger.ts:
```
extractRequestMeta(request): { ip: string | null; userAgent: string | null }
logError({level, source, message, error, code, requestId, ip, userAgent, statusCode, inputMeta})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /api/scrape-linkedin POST route + maxDuration config</name>
  <files>src/app/api/scrape-linkedin/route.ts</files>
  <read_first>
    - /Users/isaacgbc/profile-score/src/app/api/audit/stream/route.ts (FULL — verify exact `export const maxDuration` + `export const runtime` pattern)
    - /Users/isaacgbc/profile-score/src/app/api/audit/generate/route.ts (FULL — mirror POST handler structure, Zod validation, logError integration)
    - /Users/isaacgbc/profile-score/.planning/phases/01-apify-linkedin-integration/01-RESEARCH.md lines 340-390 (Pattern 4: API route shape)
    - /Users/isaacgbc/profile-score/.planning/phases/01-apify-linkedin-integration/01-RESEARCH.md lines 999-1007 (APIFY_ENABLED kill switch)
    - /Users/isaacgbc/profile-score/src/lib/services/error-logger.ts (extractRequestMeta + logError signatures)
  </read_first>
  <action>
    Step 1. Create `src/app/api/scrape-linkedin/route.ts`:
    ```
    import { NextResponse } from "next/server";
    import { createClient } from "@/lib/supabase/server"; // or whichever server-auth helper this repo uses — check via reading src/app/api/audit/generate/route.ts
    import { scrapeLinkedinRequestSchema, harvestProfileSchema } from "@/lib/schemas/linkedin-profile";
    import { extractRequestMeta, logError } from "@/lib/services/error-logger";
    import { scrapeRateLimiter } from "@/lib/services/rate-limiter";
    import { normalizeLinkedinUrl, hashUrl, readCache, writeCache } from "@/lib/services/apify-cache";
    import { computeFingerprint } from "@/lib/services/apify-fingerprint";
    import { scrapeLinkedinProfile } from "@/lib/services/apify-scraper-client";
    import { checkQuota, consumeQuota, getQuotaState, logUsage } from "@/lib/services/apify-quota";

    export const runtime = "nodejs";
    export const maxDuration = 60;

    export async function POST(request: Request) {
      const requestId = crypto.randomUUID();
      const startedAt = Date.now();
      const { ip, userAgent } = extractRequestMeta(request);

      // Kill switch
      if (process.env.APIFY_ENABLED === "false") {
        return NextResponse.json({ error: "APIFY_DISABLED" }, { status: 503 });
      }

      let normalizedUrl: string | null = null;
      let urlHash: string | null = null;
      let userId: string | null = null;
      let fingerprintHash: string | null = null;
      let cacheHit = false;
      let apifyRunId: string | null = null;
      let costUsd = 0;
      let success = false;
      let errorCode: string | null = null;

      try {
        // 1. Parse + validate body
        const body = await request.json().catch(() => ({}));
        const parsed = scrapeLinkedinRequestSchema.safeParse(body);
        if (!parsed.success) {
          errorCode = "APIFY_INVALID_URL";
          return NextResponse.json({ error: errorCode }, { status: 400 });
        }

        // 2. Auth lookup (optional — anonymous allowed)
        try {
          const supabase = await createClient();
          const { data: { user } } = await supabase.auth.getUser();
          userId = user?.id ?? null;
        } catch {
          userId = null; // anonymous fallback
        }

        // 3. Fingerprint (used as rate limit key + anon quota key)
        fingerprintHash = computeFingerprint(ip ?? "unknown", userAgent);

        // 4. Rate limit check (cost abuse defense)
        const rate = scrapeRateLimiter.check(fingerprintHash);
        if (!rate.allowed) {
          errorCode = "APIFY_RATE_LIMITED";
          return NextResponse.json(
            { error: errorCode, retryAfter: rate.retryAfter ?? 60 },
            { status: 429 }
          );
        }

        // 5. URL normalization (also validates shape — throws APIFY_INVALID_URL)
        try {
          normalizedUrl = normalizeLinkedinUrl(parsed.data.url);
        } catch {
          errorCode = "APIFY_INVALID_URL";
          return NextResponse.json({ error: errorCode }, { status: 400 });
        }
        urlHash = hashUrl(normalizedUrl);

        // 6. Cache read
        const cached = await readCache(normalizedUrl);
        let profileData: unknown;
        if (cached) {
          cacheHit = true;
          profileData = cached;
        } else {
          // 7. Quota check (only on miss)
          await checkQuota({ userId, fingerprintHash, isCacheHit: false });

          // 8. Apify call
          const result = await scrapeLinkedinProfile({ profileUrl: normalizedUrl, requestId });
          apifyRunId = result.runId;
          costUsd = result.costUsd;
          profileData = result.items[0];

          // 9. Cache write
          await writeCache({ normalizedUrl, profileData, apifyRunId, costUsd });

          // 10. Consume quota (post-scrape, only on success)
          await consumeQuota({ userId, fingerprintHash });
        }

        // 11. Return quota state for client dashboard chip
        const quotaState = await getQuotaState({ userId, fingerprintHash });

        success = true;
        return NextResponse.json({
          profile: profileData,
          cached: cacheHit,
          quotaRemaining: quotaState.remaining,
          plan: quotaState.plan,
          source: "apify",
        });
      } catch (err) {
        const raw = err instanceof Error ? err.message : String(err);
        errorCode = classifyError(raw);
        const status = statusForError(errorCode);

        logError({
          level: "error",
          source: "api/scrape-linkedin",
          message: raw,
          error: err,
          code: errorCode as any,
          statusCode: status,
          ip: ip ?? undefined,
          userAgent: userAgent ?? undefined,
          requestId,
          inputMeta: { urlHash: urlHash ?? undefined, cacheHit, costUsd, durationMs: Date.now() - startedAt },
        });

        return NextResponse.json({ error: errorCode }, { status });
      } finally {
        // 12. Append-only usage log (fire-and-forget — will not crash on failure)
        void logUsage({
          userId,
          fingerprintHash,
          url: normalizedUrl,
          urlHash,
          cacheHit,
          apifyRunId,
          costUsd,
          success,
          errorCode,
          durationMs: Date.now() - startedAt,
        });
      }
    }

    function classifyError(raw: string): string {
      const m = raw.match(/APIFY_[A-Z_]+/);
      if (m) {
        if (m[0].startsWith("APIFY_RUN_TIMED")) return "APIFY_DOWNTIME";
        if (m[0] === "APIFY_RUN_FAILED") return "APIFY_SCRAPE_FAILED";
        if (m[0] === "APIFY_TOKEN_MISSING") return "APIFY_DISABLED";
        return m[0];
      }
      return "APIFY_SCRAPE_FAILED";
    }

    function statusForError(code: string): number {
      switch (code) {
        case "APIFY_INVALID_URL": return 400;
        case "APIFY_QUOTA_EXCEEDED": return 402;
        case "APIFY_PROFILE_PRIVATE": return 422;
        case "APIFY_RATE_LIMITED": return 429;
        case "APIFY_DOWNTIME":
        case "APIFY_CIRCUIT_OPEN":
        case "APIFY_DISABLED": return 503;
        default: return 500;
      }
    }
    ```

    Step 2. If `createClient` import path differs from this repo's convention, read `src/app/api/audit/generate/route.ts` to find the actual auth helper import (likely `@/lib/supabase/server` or similar). Use that.

    Step 3. Do NOT invoke `unlock-matrix.ts` or `export-gating.ts` — per D-05, Apify gating is at the route level, separate from feature gating.
  </action>
  <verify>
    <automated>grep -q "export const runtime = \"nodejs\"" src/app/api/scrape-linkedin/route.ts && grep -q "export const maxDuration = 60" src/app/api/scrape-linkedin/route.ts && grep -q "export async function POST" src/app/api/scrape-linkedin/route.ts && grep -q "APIFY_ENABLED" src/app/api/scrape-linkedin/route.ts && grep -q "scrapeRateLimiter.check" src/app/api/scrape-linkedin/route.ts && grep -q "scrapeLinkedinProfile" src/app/api/scrape-linkedin/route.ts && grep -q "checkQuota" src/app/api/scrape-linkedin/route.ts && grep -q "consumeQuota" src/app/api/scrape-linkedin/route.ts && grep -q "logUsage" src/app/api/scrape-linkedin/route.ts && grep -q "readCache" src/app/api/scrape-linkedin/route.ts && grep -q "writeCache" src/app/api/scrape-linkedin/route.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File `src/app/api/scrape-linkedin/route.ts` exists
    - `grep -q "maxDuration = 60" src/app/api/scrape-linkedin/route.ts` succeeds
    - `grep -q "runtime = \"nodejs\"" src/app/api/scrape-linkedin/route.ts` succeeds
    - `grep -q "APIFY_ENABLED" src/app/api/scrape-linkedin/route.ts` succeeds
    - `grep -q "APIFY_INVALID_URL" src/app/api/scrape-linkedin/route.ts` succeeds
    - `grep -q "APIFY_QUOTA_EXCEEDED" src/app/api/scrape-linkedin/route.ts` succeeds
    - `grep -q "APIFY_PROFILE_PRIVATE" src/app/api/scrape-linkedin/route.ts` succeeds
    - `grep -q "APIFY_RATE_LIMITED" src/app/api/scrape-linkedin/route.ts` succeeds
    - `grep -q "APIFY_CIRCUIT_OPEN\\|APIFY_DOWNTIME" src/app/api/scrape-linkedin/route.ts` succeeds
    - `grep -q "logError" src/app/api/scrape-linkedin/route.ts` succeeds
    - `grep -q "logUsage" src/app/api/scrape-linkedin/route.ts` succeeds (in finally block)
    - `grep -q "extractRequestMeta" src/app/api/scrape-linkedin/route.ts` succeeds
    - `grep -q "computeFingerprint" src/app/api/scrape-linkedin/route.ts` succeeds
    - Route does NOT import `unlock-matrix` or `export-gating` (`grep -L "unlock-matrix\\|export-gating" src/app/api/scrape-linkedin/route.ts` — should NOT appear)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Route compiles, all 10+ imports resolve, maxDuration declared, all error branches handled, build clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration test for /api/scrape-linkedin route (mocked Apify + Prisma)</name>
  <files>src/app/api/scrape-linkedin/__tests__/route.test.ts</files>
  <read_first>
    - /Users/isaacgbc/profile-score/src/app/api/scrape-linkedin/route.ts (just created)
    - /Users/isaacgbc/profile-score/src/lib/db/__mocks__/client.ts (from Plan 01-03)
    - /Users/isaacgbc/profile-score/src/lib/services/__tests__/fixtures/apify-profile-sample.json
    - /Users/isaacgbc/profile-score/src/lib/services/__tests__/fixtures/apify-profile-empty.json
  </read_first>
  <behavior>
    - Test 1: Valid URL + fresh call + mocked successful Apify returns 200 with `{profile, cached: false, quotaRemaining, plan, source: "apify"}`
    - Test 2: Valid URL + cache hit (pre-seeded) returns 200 with `cached: true` and does NOT call the Apify client mock
    - Test 3: Invalid URL (non-linkedin host) returns 400 with `{error: "APIFY_INVALID_URL"}`
    - Test 4: Invalid URL (data: URI) returns 400 with `{error: "APIFY_INVALID_URL"}`
    - Test 5: `APIFY_ENABLED=false` returns 503 with `{error: "APIFY_DISABLED"}`
    - Test 6: Rate limit exceeded (4th call same fingerprint in 1 min) returns 429 with `retryAfter`
    - Test 7: Anonymous user hits free quota (2nd scrape of different URL) returns 402 with `{error: "APIFY_QUOTA_EXCEEDED"}`
    - Test 8: Apify returns empty items → route returns 422 with `{error: "APIFY_PROFILE_PRIVATE"}`
    - Test 9: Apify run status FAILED → route returns 500 with `{error: "APIFY_SCRAPE_FAILED"}`
    - Test 10: Circuit breaker OPEN → route returns 503 with `{error: "APIFY_CIRCUIT_OPEN"}`
    - Test 11: APIFY_API_TOKEN missing → route returns 503 with `{error: "APIFY_DISABLED"}`
    - Test 12: Every error branch calls `logError` once with correct source
    - Test 13: Every request (success or error) results in ONE `apifyUsageLog.create` call (finally block)
    - Test 14: Authenticated paid user (apifyScrapeQuota=null) succeeds unconditionally after cache miss
  </behavior>
  <action>
    Step 1. Create `src/app/api/scrape-linkedin/__tests__/route.test.ts`:
    - Top mocks:
      ```
      vi.mock("@/lib/db/client", () => import("@/lib/db/__mocks__/client"));
      vi.mock("apify-client", () => ({ ApifyClient: vi.fn() }));
      vi.mock("@/lib/services/error-logger", async () => {
        const actual = await vi.importActual<any>("@/lib/services/error-logger");
        return { ...actual, logError: vi.fn(), extractRequestMeta: vi.fn(() => ({ ip: "1.2.3.4", userAgent: "test-agent" })) };
      });
      // Mock the auth helper to return { data: { user: null } } by default, override per-test
      vi.mock("@/lib/supabase/server", () => ({ createClient: vi.fn(async () => ({ auth: { getUser: async () => ({ data: { user: null } }) } })) }));
      ```
    - For each test:
      - Reset the Prisma mock store via `(prisma as any).__test__.reset()`
      - Set up ApifyClient mock return value (actor -> call -> `{status, defaultDatasetId, id, usageTotalUsd}`; dataset -> listItems -> `{items: [...]}`)
      - Build a `new Request("http://localhost/api/scrape-linkedin", { method: "POST", body: JSON.stringify({url: "https://linkedin.com/in/test"}), headers: {...} })`
      - Call `POST(request)` directly and inspect `response.status` + `await response.json()`
    - Use fixtures from `src/lib/services/__tests__/fixtures/apify-profile-sample.json` for successful returns and `apify-profile-empty.json` for private-profile tests
    - For rate limit tests, use unique fingerprints per test OR reset the limiter state if possible
    - For paid user tests, `__test__.seedUser({id: "user-1", email: "a@b.com", apifyScrapeQuota: null, activePlanId: "starter"})` then override the auth mock to return `{data: {user: {id: "user-1"}}}`

    Step 2. All 14 behaviors must be implemented as individual `it()` blocks.

    Step 3. At end of each test, assert `logUsage` was called OR manually verify `(prisma as any).__test__.logStore.length` changed (since logUsage is called in finally). For tests expecting error responses, also assert `logError` mock was called.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/scrape-linkedin/__tests__/route.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - File `src/app/api/scrape-linkedin/__tests__/route.test.ts` exists
    - `grep -q "vi.mock" src/app/api/scrape-linkedin/__tests__/route.test.ts` succeeds
    - `grep -q "APIFY_PROFILE_PRIVATE" src/app/api/scrape-linkedin/__tests__/route.test.ts` succeeds
    - `grep -q "APIFY_QUOTA_EXCEEDED" src/app/api/scrape-linkedin/__tests__/route.test.ts` succeeds
    - `grep -q "APIFY_RATE_LIMITED\\|429" src/app/api/scrape-linkedin/__tests__/route.test.ts` succeeds
    - `grep -q "APIFY_DISABLED" src/app/api/scrape-linkedin/__tests__/route.test.ts` succeeds
    - `npx vitest run src/app/api/scrape-linkedin/__tests__/route.test.ts` shows 14 passing tests
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>14 integration tests cover all error + success paths for /api/scrape-linkedin.</done>
</task>

<task type="auto">
  <name>Task 3: Add apifyScrapeQuota:null 1-line update to Creala webhook + regression test</name>
  <files>src/app/api/webhooks/creala/route.ts, src/app/api/webhooks/creala/__tests__/route.test.ts</files>
  <read_first>
    - /Users/isaacgbc/profile-score/src/app/api/webhooks/creala/route.ts (FULL — read every line, locate the `prisma.user.update({where:..., data:{...}})` calls inside the `new_sale` and `new_subscription` case branches)
    - /Users/isaacgbc/profile-score/.planning/phases/01-apify-linkedin-integration/01-CONTEXT.md D-14
    - /Users/isaacgbc/profile-score/.planning/phases/01-apify-linkedin-integration/01-RESEARCH.md lines 396-399 (anti-pattern warning — DO NOT refactor HMAC or handler shape)
  </read_first>
  <action>
    Step 1. **Surgical edit only** to `src/app/api/webhooks/creala/route.ts`:
    - Locate every `prisma.user.update({where: ..., data: {...}})` call that runs in a successful `new_sale` or `new_subscription` branch (where the user's plan is being upgraded to `starter` or `recommended`)
    - Inside each matching `data: {...}` object, add ONE new property: `apifyScrapeQuota: null,`
    - Do NOT modify: HMAC verification, signature header parsing, timing-safe equality, handler export, the case branch structure, any non-user-update prisma call, any logging, any response shape, any other field in the `data:` object
    - Add a comment above each insertion: `// Plan 01-07: reset Apify quota on plan upgrade (D-14)`

    Step 2. If there are TWO user updates (one for `new_sale`, one for `new_subscription`), add the property to BOTH. If there is ONE update shared by both cases, add it once in that update.

    Step 3. Create `src/app/api/webhooks/creala/__tests__/route.test.ts` as a regression test:
    - Test 1: `prisma.user.update` data object contains `apifyScrapeQuota: null` after processing a mock `new_sale` webhook (verify via seeded user starting with `apifyScrapeQuota: 1` then asserting post-webhook state has `apifyScrapeQuota: null`)
    - Test 2: Same for `new_subscription` (if that case exists in the real handler)
    - Test 3: Regression guard — HMAC verification is still called (check that a request with an invalid signature returns an error status, NOT 200) — this proves Step 1 did not break HMAC
    - Test 4: Regression guard — the handler still updates `activePlanId` and `subscriptionStatus` on successful plan update (pre-existing behavior preserved)
    - Mocks:
      - `vi.mock("@/lib/db/client", () => import("@/lib/db/__mocks__/client"));`
      - Mock whatever Creala SDK imports exist
      - Seed a test user via `__test__.seedUser`
      - Call the POST handler directly with a forged-but-HMAC-valid request body (use the real `CREALA_WEBHOOK_SECRET` env var set in test setup, OR mock the verify function)

    Step 4. If the Creala route uses `verifySignature` or similar, and the test can't easily forge HMAC, the test can mock the verification function to return `true` for test purposes — but must NOT modify the verification function itself. Mocking at the import boundary is acceptable.
  </action>
  <verify>
    <automated>grep -q "apifyScrapeQuota: null" src/app/api/webhooks/creala/route.ts && grep -q "Plan 01-07" src/app/api/webhooks/creala/route.ts && npx vitest run src/app/api/webhooks/creala/__tests__/route.test.ts --reporter=verbose && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "apifyScrapeQuota: null" src/app/api/webhooks/creala/route.ts` succeeds
    - `grep -q "Plan 01-07" src/app/api/webhooks/creala/route.ts` succeeds (comment marker)
    - Number of lines in webhook file increased by exactly 2-4 (1 comment + 1 property per update call, at most 2 update calls)
    - `grep -q "verifySignature\\|CREALA_WEBHOOK_SECRET\\|hmac\\|createHmac" src/app/api/webhooks/creala/route.ts` still succeeds (HMAC logic unchanged — same as before)
    - `grep -q "activePlanId" src/app/api/webhooks/creala/route.ts` still succeeds (existing field untouched)
    - Test file `src/app/api/webhooks/creala/__tests__/route.test.ts` exists
    - `npx vitest run src/app/api/webhooks/creala/__tests__/route.test.ts` shows at least 3 passing tests (Test 1 quota reset + Test 3 HMAC regression + Test 4 plan update regression; Test 2 optional if new_subscription case exists)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Creala webhook updated with single-line quota reset, HMAC verification unchanged, regression tests prove it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> /api/scrape-linkedin | Untrusted input (URL); Zod + normalization validate |
| Supabase Auth -> route | userId from server-verified session, never from request body |
| Route -> Apify | Server-to-server call with server-only token |
| Creala -> webhook | HMAC-SHA256 verified (UNCHANGED — do not touch) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 (T-09: SSRF via crafted URL) | Information Disclosure | Route validation | mitigate | Double gate: Zod schema rejects non-linkedin hosts (Plan 01-01 tests 4-8); `normalizeLinkedinUrl` throws `APIFY_INVALID_URL` on any non-linkedin host (Plan 01-03 tests 7-9). Route catches both. |
| T-07-02 (T-04: rate-limit bypass via header spoofing) | Elevation of Privilege | Rate limiter | mitigate | Limiter key is SHA-256 fingerprint of `ip + userAgent + salt` via `computeFingerprint`, not raw `X-Forwarded-For`. An attacker spoofing one header still has the other + salt as variables. |
| T-07-03 (T-08: auth bypass on plan-gating) | Elevation of Privilege | Quota check | mitigate | `userId` is resolved via `supabase.auth.getUser()` server-side, never from request body. `checkQuota` reads `apifyScrapeQuota` from the authoritative DB. Paid status can never be claimed by the client. |
| T-07-04 (T-05: HMAC bypass on Creala webhook) | Tampering | Creala handler | mitigate | This plan's Creala edit is ONE property inside the existing `data:` object, INSIDE the already-verified HMAC check block. Zero changes to verifySignature, timing-safe equality, handler signature, or any other logic. Regression Test 3 proves HMAC still rejects invalid signatures post-edit. |
| T-07-05 (T-12: cost abuse via high-volume scrape triggering) | DoS / cost | Route | mitigate | Layered: (1) `APIFY_ENABLED=false` kill switch short-circuits at line 1, (2) rate limiter 3/min per fingerprint, (3) fingerprint quota 1 scrape lifetime, (4) cache lookup avoids re-scrape of same URL within 24h, (5) circuit breaker prevents cascading failures. |
| T-07-06 (T-07: log injection via LinkedIn URL) | Tampering | `logError.inputMeta` | mitigate | `inputMeta` only contains `urlHash` (SHA-256), NOT the raw URL. `durationMs`, `cacheHit`, `costUsd` are numbers. No user-controlled string interpolation in log format strings. |
| T-07-07 (T-02: PII exposure via route response) | Information Disclosure | Response body | mitigate | Response contains the scraped profile data — THIS IS INTENTIONAL (client needs it to run the audit). Mitigation: cache stores for max 24h; route is rate-limited; quota gated; never serve to a different user than the one who paid for the scrape (cache key is URL-based, not user-based, but all subsequent readers also burn quota — acceptable residual). |
| T-07-08 (T-06: cascading failure when Apify down) | DoS | Error handling | mitigate | `APIFY_CIRCUIT_OPEN` mapped to 503 + `apify.error.downtime` i18n key. UI surfaces the "Try CV upload" fallback action. Circuit breaker prevents thundering herd. |
| T-07-09 (Unhandled exception returns 500 with stack) | Information Disclosure | `classifyError` | mitigate | Default case returns generic `APIFY_SCRAPE_FAILED`; raw error messages never returned to client (only logged server-side via logError). |
</threat_model>

<verification>
- [ ] `npx vitest run src/app/api/scrape-linkedin/__tests__/route.test.ts src/app/api/webhooks/creala/__tests__/route.test.ts` shows 17+ passing tests
- [ ] `npm run build` exits 0
- [ ] `/api/scrape-linkedin` route declares `maxDuration = 60`
- [ ] Creala webhook HMAC verification is untouched (regression test proves)
- [ ] Every error branch returns a typed error code matching i18n keys
</verification>

<success_criteria>
1. POST /api/scrape-linkedin accepts URL, orchestrates the full pipeline, returns typed JSON
2. All 7 error codes map to correct HTTP statuses (400, 402, 422, 429, 503, 503, 500)
3. Cache hits bypass quota consumption
4. Auth resolves userId server-side (never trusted from client)
5. Creala webhook sets `apifyScrapeQuota: null` on plan upgrade (1-line change, no refactor)
6. logUsage is called in finally block for every request
7. 14 route tests + 3+ Creala regression tests pass, build clean
</success_criteria>

<output>
After completion, create `.planning/phases/01-apify-linkedin-integration/01-07-SUMMARY.md` with:
- Exact supabase server-auth import path used (for downstream reference)
- Number of Creala user.update sites modified (should be 1 or 2)
- Confirmation that HMAC verify function line count is unchanged
- Total test count (17+)
</output>