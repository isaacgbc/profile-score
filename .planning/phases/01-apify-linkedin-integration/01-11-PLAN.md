---
phase: 01-apify-linkedin-integration
plan: 11
type: execute
wave: 7
depends_on:
  - "01-08"
  - "01-09"
  - "01-10"
files_modified:
  - src/lib/services/__tests__/integration-apify-full-flow.test.ts
autonomous: false
requirements:
  - REQ-01
  - REQ-02
  - REQ-03
  - REQ-04
must_haves:
  truths:
    - "End-to-end integration test simulates full flow: URL → scrape → cache → orchestrator → audit result"
    - "Test covers cached-path (no Apify call) and cache-miss path (Apify call mocked)"
    - "Full vitest suite runs green"
    - "npm run build exits 0 with zero new warnings"
    - "Isaac manually verifies the complete flow in dev (EN + ES) and both paths (LinkedIn URL + CV upload)"
    - "APIFY_API_TOKEN is set in Vercel env (production + preview + development)"
  artifacts:
    - path: "src/lib/services/__tests__/integration-apify-full-flow.test.ts"
      provides: "End-to-end integration test covering the entire Apify pipeline (mocked Apify SDK + mocked LLM)"
  key_links:
    - from: "src/lib/services/__tests__/integration-apify-full-flow.test.ts"
      to: "src/app/api/scrape-linkedin/route.ts"
      via: "POST handler direct call"
      pattern: "POST"
    - from: "src/lib/services/__tests__/integration-apify-full-flow.test.ts"
      to: "src/lib/services/audit-orchestrator.ts"
      via: "orchestrator invoked with preparsedLinkedinSections"
      pattern: "preparsedLinkedinSections"
---

<objective>
Verify the entire Apify pipeline works end-to-end by running a full integration test that chains the scrape route → cache → orchestrator → audit result in a single test, plus a manual smoke checkpoint that Isaac runs against a real deployment (or `npm run dev`) with real credentials. This is the final wave — the phase completes when all verification is green and the manual checkpoint is approved.

Purpose: Individual unit + integration tests from Plans 01-01 through 01-10 prove each component works in isolation. This plan proves they work TOGETHER. It also exercises the 9 ROADMAP success criteria as the last gate before `/gsd-verify-work` is run.

Output: One comprehensive integration test file + manual verification checkpoint covering all 9 ROADMAP success criteria + phase-level build gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-apify-linkedin-integration/01-CONTEXT.md
@.planning/phases/01-apify-linkedin-integration/01-RESEARCH.md
@.planning/phases/01-apify-linkedin-integration/01-VALIDATION.md
@.planning/ROADMAP.md
@CLAUDE.md
@src/app/api/scrape-linkedin/route.ts
@src/lib/services/audit-orchestrator.ts
@src/lib/services/linkedin-profile-formatter.ts
@src/lib/services/__tests__/fixtures/apify-profile-sample.json

<interfaces>
From Plan 01-07 route: `POST /api/scrape-linkedin` accepts `{url}`, returns `{profile, cached, quotaRemaining, plan, source}`.
From Plan 01-08 orchestrator: accepts `{preparsedLinkedinSections, linkedinProfileSource: "apify"}` and bypasses Stage 2 parser.
From Plan 01-05 formatter: `profileToSectionRecord(p)` converts HarvestProfile to the section record.
From Plan 01-04 scraper client: mockable via `vi.mock("apify-client", ...)`.
From Plan 01-03 Prisma mock: `src/lib/db/__mocks__/client.ts` with seedUser/reset helpers.
From ROADMAP.md §Phase 1 Success Criteria: 9 measurable criteria (re-listed in `<behavior>` block below).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: End-to-end integration test chaining scrape route → orchestrator → audit result</name>
  <files>src/lib/services/__tests__/integration-apify-full-flow.test.ts</files>
  <read_first>
    - /Users/isaacgbc/profile-score/src/app/api/scrape-linkedin/route.ts (from Plan 01-07)
    - /Users/isaacgbc/profile-score/src/lib/services/audit-orchestrator.ts (post-Plan 01-08)
    - /Users/isaacgbc/profile-score/src/lib/services/linkedin-profile-formatter.ts (from Plan 01-05)
    - /Users/isaacgbc/profile-score/.planning/phases/01-apify-linkedin-integration/01-VALIDATION.md §Per-Task Verification Map
    - /Users/isaacgbc/profile-score/.planning/ROADMAP.md lines 26-36 (the 9 Success Criteria)
    - /Users/isaacgbc/profile-score/src/lib/services/__tests__/fixtures/apify-profile-sample.json
  </read_first>
  <behavior>
    ROADMAP success criteria mapped to tests. Automate as many as possible; the rest go into the manual checkpoint.
    - **SC-1 (URL input triggers Apify scrape)**: Test 1 — POST to route with valid URL, mocked Apify returns fixture; assert route returns 200 with profile data and `source === "apify"`
    - **SC-2 (structured data produces richer audit)**: Test 2 — End-to-end chain: route returns profile → call formatter → invoke orchestrator with `preparsedLinkedinSections` + `linkedinProfileSource: "apify"` (mocked LLM); assert orchestrator output has the expected shape AND `structuringUsed === true` AND the Apify-variant prompt was resolved
    - **SC-3 (CV upload preserved — regression)**: Test 3 — Invoke orchestrator with `linkedinText: "..."` (no preparsedLinkedinSections); assert the old parser path is called and produces output. THIS IS THE CV/PASTE REGRESSION GUARD.
    - **SC-4 (cache hit avoids scrape)**: Test 4 — First POST caches the profile; second POST with same URL returns 200 with `cached: true` AND the ApifyClient mock's `.call()` was invoked ONCE total (not twice)
    - **SC-4 (cache TTL)**: Test 5 — Seed a cache entry with `expiresAt` in the past; POST with same URL triggers a fresh Apify call (cache miss on TTL expiry)
    - **SC-5 (free quota: 1 scrape)**: Test 6 — First POST with anon fingerprint succeeds; second POST with DIFFERENT URL but same fingerprint returns 402 with `APIFY_QUOTA_EXCEEDED`
    - **SC-5 (paid unlimited)**: Test 7 — Seed user with `apifyScrapeQuota: null`, mock auth to return that user; 3 scrapes in a row all succeed
    - **SC-6 (4 error states)**: Tests 8-11 — Private (empty items), Invalid URL (non-linkedin host), Rate limit (4 calls in 1 min), Downtime (circuit open). Each returns the correct HTTP status + error code.
    - **SC-7 (bilingual output)**: Covered by existing language-detect tests; this test asserts the orchestrator receives + processes both EN and ES locales without error (Test 12)
    - **SC-8 (build passes)**: CI gate — `npm run build`; verified in Task 2 manual checkpoint
    - **SC-9 (Creala untouched)**: Covered by Plan 01-07 Creala regression tests; Test 13 here re-asserts by querying the HMAC verify function source line count pre/post (informational — already enforced by Plan 01-07)
  </behavior>
  <action>
    Step 1. Create `src/lib/services/__tests__/integration-apify-full-flow.test.ts`:
    - Top of file:
      ```
      import { describe, it, expect, beforeEach, vi } from "vitest";
      import { POST as scrapeLinkedinPOST } from "@/app/api/scrape-linkedin/route";
      import { prisma } from "@/lib/db/client"; // resolves to mock via vi.mock below
      import sampleFixture from "./fixtures/apify-profile-sample.json";
      import emptyFixture from "./fixtures/apify-profile-empty.json";
      import { profileToSectionRecord } from "@/lib/services/linkedin-profile-formatter";
      // orchestrator import if test invokes it directly
      import { runAuditOrchestrator } from "@/lib/services/audit-orchestrator"; // adjust name to real export

      vi.mock("@/lib/db/client", () => import("@/lib/db/__mocks__/client"));
      vi.mock("apify-client", () => ({ ApifyClient: vi.fn() }));
      // Mock LLM client
      vi.mock("@/lib/services/llm-client", () => ({
        callLLM: vi.fn(async () => ({ content: "mocked audit output" })),
        // match whatever the real llm-client exports
      }));
      // Mock prompt resolver to return fake prompts
      vi.mock("@/lib/services/prompt-resolver", () => ({
        getActivePromptWithVersion: vi.fn(async (key: string) => ({
          promptKey: key, version: 1, locale: "en", status: "active", body: "You are an auditor. " + key,
        })),
      }));
      // Mock supabase auth
      vi.mock("@/lib/supabase/server", () => ({
        createClient: vi.fn(async () => ({ auth: { getUser: async () => ({ data: { user: null } }) } })),
      }));
      ```
    - Helper to build a request:
      ```
      function buildReq(url: string) {
        return new Request("http://localhost/api/scrape-linkedin", {
          method: "POST",
          headers: { "Content-Type": "application/json", "x-forwarded-for": "1.2.3.4", "user-agent": "test-agent" },
          body: JSON.stringify({ url }),
        });
      }
      ```
    - `beforeEach`: reset Prisma mock store, reset Apify mock, reset rate limiter state (use unique fingerprints per test OR import and reset the limiter), reset circuit breaker state (expose via test hook if needed)
    - Implement all 12 tests from the `<behavior>` block.
    - Test 2 is the KEY test — after the scrape succeeds, the test calls `profileToSectionRecord(res.profile)` then invokes the orchestrator directly with `{preparsedLinkedinSections: sections, linkedinProfileSource: "apify", locale: "en", ...otherRequiredInputs}` and asserts the result.
    - For Test 12 (bilingual), run the orchestrator twice with `locale: "en"` and `locale: "es"` using mocked prompts that return locale-specific body — assert both return valid results.
    - For Test 13 (Creala untouched), simply assert via `fs.readFileSync` that `src/app/api/webhooks/creala/route.ts` contains `verifySignature` or `createHmac` (proving the HMAC function is still there) AND contains `apifyScrapeQuota: null` (proving Plan 01-07's edit was applied).

    Step 2. At the end of the test file, add a `describe("Phase 01 roadmap acceptance", () => { ... })` summary block that logs "Phase 01 roadmap acceptance: 9/9 success criteria exercised" when all tests pass. This is for readability in test output, not a real assertion.

    Step 3. Wire the test file into the full suite: running `npx vitest run` should include it by default (it's under `src/**/*.test.ts`).

    Step 4. Fix any mock shape drift discovered while writing this test — update `src/lib/db/__mocks__/client.ts` if needed to support new test scenarios. Do NOT modify real source files to accommodate test mocks.
  </action>
  <verify>
    <automated>npx vitest run src/lib/services/__tests__/integration-apify-full-flow.test.ts --reporter=verbose && npx vitest run && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File `src/lib/services/__tests__/integration-apify-full-flow.test.ts` exists
    - `grep -q "preparsedLinkedinSections" src/lib/services/__tests__/integration-apify-full-flow.test.ts` succeeds
    - `grep -q "profileToSectionRecord" src/lib/services/__tests__/integration-apify-full-flow.test.ts` succeeds
    - `grep -q "APIFY_QUOTA_EXCEEDED" src/lib/services/__tests__/integration-apify-full-flow.test.ts` succeeds
    - `grep -q "APIFY_PROFILE_PRIVATE" src/lib/services/__tests__/integration-apify-full-flow.test.ts` succeeds
    - `grep -q "cached: true\\|\"cached\".*true" src/lib/services/__tests__/integration-apify-full-flow.test.ts` succeeds (cache hit assertion)
    - `grep -q "apifyScrapeQuota: null" src/lib/services/__tests__/integration-apify-full-flow.test.ts` succeeds (paid tier test)
    - `grep -q "verifySignature\\|createHmac" src/lib/services/__tests__/integration-apify-full-flow.test.ts` succeeds (Creala regression assertion)
    - `npx vitest run src/lib/services/__tests__/integration-apify-full-flow.test.ts` shows 13 passing tests
    - `npx vitest run` (full suite) exits 0 with all tests green (total should be 100+ tests across all phase plans)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Full integration test chains all components, exercises 8/9 success criteria automatically, full suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: CHECKPOINT — Isaac runs full manual smoke test against real Apify</name>
  <files>.env.local, src/app/input/page.tsx, src/app/results/page.tsx</files>
  <action>CHECKPOINT — final phase gate. Isaac runs the full suite and manual E2E covering all 9 ROADMAP success criteria. Pre-flight: confirm .env.local + Vercel env vars, confirm db schema synced. Gates: npx vitest run (all green), npm run build (zero errors), npm run lint. E2E: submit real LinkedIn URL in dev (EN + ES), verify CV regression, verify cache hit path, verify free quota exhaustion, verify dashboard chip for free + paid tiers, verify all 4 error states localized in both languages, verify Creala webhook still updates User.apifyScrapeQuota=null on plan upgrade. Deploy to Vercel preview and measure real Apify p95 latency — if greater than 55s, flag async pattern as follow-up. Full instructions in how-to-verify below.</action>
  <verify><automated>npx vitest run && npm run build</automated></verify>
  <done>Isaac reports "approved: 9/9 pass" with SC-1 through SC-9 pass/fail table. Any DEFERRED criterion is documented with justification. Phase COMPLETE.</done>
  <what-built>All code is in place: schema migrated, services built, route live, UI wired, tests green. Task 1 proved the integration works with mocks. This checkpoint proves it works against the REAL Apify API with real credentials.</what-built>
  <how-to-verify>
    ### Pre-flight setup
    1. **Confirm `.env.local`** contains:
       - `APIFY_API_TOKEN=apify_api_<real-value>`
       - `APIFY_FINGERPRINT_SALT=<32-byte-hex>`
       - `APIFY_ENABLED=true`
    2. **Confirm Vercel env vars** contain the same values (Production + Preview + Development scopes)
    3. **Confirm database schema** is synced: `npm run db:push` reports "in sync"

    ### Full suite gate
    4. Run `npx vitest run` — ALL tests must pass (expect 100+ tests across 11 plans)
    5. Run `npm run build` — MUST exit 0 with ZERO new warnings
    6. Run `npm run lint` — should exit 0

    ### Manual E2E in dev
    7. Start dev server: `npm run dev`
    8. Visit http://localhost:3000/input
    9. **SC-1, SC-2, SC-7 — EN flow**:
       - Enter a known-public LinkedIn URL (e.g., your own profile)
       - Submit → confirm "Analyzing your LinkedIn profile..." loading state appears FIRST
       - Wait for audit to complete → confirm results page renders
       - Compare audit richness against a paste-based v1 audit (qualitative check — is the new output visibly more detailed?)
       - Confirm output language matches page language (EN)
    10. **SC-7 — ES flow**: switch to ES (lang toggle or `?lang=es`), repeat step 9 with a Spanish locale
    11. **SC-3 — CV regression**: on /input, expand the "Prefer to upload your CV?" details, upload a sample CV PDF, confirm the audit flow still works end-to-end
    12. **SC-4 — cache**: submit the same URL twice within a minute. Second call should be near-instant (cache hit). Check Vercel logs or `prisma.linkedInProfileCache.findMany()` to confirm.
    13. **SC-5 — free quota**: in incognito (new anon fingerprint), submit ONE URL successfully, then submit a DIFFERENT URL — expect `apify.error.quotaExceeded` + "Upgrade plan" CTA
    14. **SC-5 — dashboard chip**: confirm `/results` shows "Escaneos restantes: 0" after the free quota is used. For a paid user (log in with an account that has a starter/recommended plan OR manually set `apifyScrapeQuota: null` via prisma studio), confirm "Escaneos ilimitados"
    15. **SC-6 — error states** (each MUST show localized copy in EN AND ES):
        - **Invalid URL**: submit `https://google.com/in/foo` → expect `apify.error.invalidUrl` banner
        - **Private profile**: submit a known-private LinkedIn URL → expect `apify.error.private` + "Upload CV instead" CTA
        - **Rate limit**: rapid-fire 4 submissions in under 60s → expect `apify.error.rateLimit`
        - **Downtime**: set `APIFY_ENABLED=false` in `.env.local`, restart dev, submit any URL → expect `apify.error.downtime` (or `apify.error.disabled`)
    16. **SC-9 — Creala untouched**: trigger a test Creala webhook (or simulate via `curl` with valid HMAC); confirm user's `apifyScrapeQuota` flips to `null` and the dashboard chip shows "Escaneos ilimitados"
    17. **SC-8 — final build gate**: `npm run build` exits 0

    ### Vercel Lambda timeout observation
    18. Deploy to Vercel preview: `vercel` (or push to a PR branch). On the preview URL, run a real scrape. Observe Vercel Function logs for the `/api/scrape-linkedin` execution time. If p95 > 55s, flag as follow-up (deferred async pattern from 01-RESEARCH.md §Pitfall 1).

    ### Sign-off
    Report each SC-1 through SC-9 as PASS, FAIL, or DEFERRED. A single FAIL blocks phase completion. A DEFERRED must be documented with justification.
  </how-to-verify>
  <resume-signal>Type "approved: 9/9 pass" if all criteria pass. Type "approved with deferrals: <list>" if some are deferred with justification. Type "failed: <which SC>" with reproduction steps if blocked.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test suite -> source modules | Test code may import internals; no external network |
| Manual smoke -> real Apify | Real API credentials in use; real scrape costs real money (~$0.02 for full smoke run) |
| Manual smoke -> real Creala | NO — use test webhook, not real Creala API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 (Real API token leaks via test artifacts) | Information Disclosure | Integration test | mitigate | Test uses `vi.mock("apify-client", ...)` — never calls real Apify. Real token only touched during manual smoke (Task 2), which runs in Isaac's local env, never committed. `.env.local` is in `.gitignore` (verified by existing repo hygiene). |
| T-11-02 (Smoke test triggers real Creala transaction) | Financial loss | Manual smoke SC-9 | accept | Instruction says "test Creala webhook or simulate via curl" — Isaac knows to use test/staging webhook, not production. Real Creala test can be done via admin's own test account if available. |
| T-11-03 (Cache poisoning from smoke test data) | Tampering | LinkedInProfileCache in dev DB | accept | Dev database is isolated from production. Smoke test writes can be cleaned via `prisma.linkedInProfileCache.deleteMany()` after testing. |
| T-11-04 (Test suite flakiness masking regressions) | Integrity | Test file | mitigate | Fake timers used where time is relevant (cache TTL, rate limit window). Unique fingerprints per test to avoid cross-test state leakage. Prisma mock reset in `beforeEach`. |
| T-11-05 (Build gate false positive from cached .next dir) | Integrity | `npm run build` | mitigate | Instruct to verify build output mentions "Compiled successfully" and Prisma client regeneration runs (automatic via `prisma generate` in build script). If in doubt, `rm -rf .next node_modules/.prisma && npm run build`. |
| T-11-06 (Missing APIFY_API_TOKEN in Vercel prevents prod deploy) | Availability | Deployment | mitigate | Task 2 step 2 explicitly requires confirming Vercel env vars before deploy. Deploy without token would cause 503 `APIFY_DISABLED` responses — detectable immediately. |
</threat_model>

<verification>
- [ ] 13 new integration tests pass
- [ ] Full `npx vitest run` suite green (100+ tests expected)
- [ ] `npm run build` exits 0 with zero warnings
- [ ] Manual smoke covered all 9 ROADMAP success criteria
- [ ] Isaac signed off with "approved: 9/9 pass" or equivalent
</verification>

<success_criteria>
1. Integration test chains scrape route → cache → orchestrator → audit output
2. All 9 ROADMAP success criteria exercised (7-8 automated, 1-2 manual)
3. Full vitest suite is green (phase-level gate)
4. Build clean
5. Manual checkpoint approved with explicit pass/fail per SC
6. Real Apify API tested against a public profile (de-risks all Wave 0 assumptions)
</success_criteria>

<output>
After completion, create `.planning/phases/01-apify-linkedin-integration/01-11-SUMMARY.md` with:
- Final total test count across all 11 plans
- SC-1 through SC-9 pass/fail table with notes
- Vercel Lambda p95 latency for a real scrape (actual observed number)
- Any assumptions A1-A10 from 01-RESEARCH.md that were CONFIRMED or REFUTED during smoke
- List of deferred follow-ups (should include admin Apify analytics from D-27, async run-then-poll pattern if p95 was high)
- Phase completion declaration: "Phase 01 COMPLETE — ready for /gsd-verify-work"
</output>