---
phase: 01-apify-linkedin-integration
plan: 10
type: execute
wave: 5
depends_on:
  - "01-07"
  - "01-09"
files_modified:
  - src/context/AppContext.tsx
  - src/components/results/ApifyQuotaChip.tsx
  - src/components/results/__tests__/ApifyQuotaChip.test.tsx
  - src/app/results/page.tsx
  - src/hooks/useLinkedinScrape.ts
  - package.json
  - vitest.config.ts
  - vitest.setup.ts
autonomous: true
requirements:
  - REQ-04
  - REQ-03
must_haves:
  truths:
    - "AppContext exposes scrapedProfile, scrapeStatus, scrapeError, apifyQuotaRemaining, triggerLinkedinScrape"
    - "ApifyQuotaChip renders 'Escaneos restantes: X' for free users and 'Escaneos ilimitados' for paid users"
    - "Chip shows on /results page via AppContext"
    - "Chip uses CSS variables only, no raw Tailwind colors"
    - "Component test verifies both states (remaining count + unlimited) + i18n parity"
  artifacts:
    - path: "src/context/AppContext.tsx"
      provides: "5 new state fields for apify + triggerLinkedinScrape dispatcher"
    - path: "src/components/results/ApifyQuotaChip.tsx"
      provides: "Dashboard chip component"
      exports: ["ApifyQuotaChip"]
    - path: "src/components/results/__tests__/ApifyQuotaChip.test.tsx"
      provides: "Component test for free/paid rendering"
  key_links:
    - from: "src/components/results/ApifyQuotaChip.tsx"
      to: "src/context/AppContext.tsx"
      via: "useAppContext() hook consumption"
      pattern: "useAppContext|apifyQuotaRemaining"
    - from: "src/app/results/page.tsx"
      to: "src/components/results/ApifyQuotaChip.tsx"
      via: "component import + render"
      pattern: "ApifyQuotaChip"
---

<objective>
Centralize the Apify-related client state in `AppContext.tsx` so the input page (Plan 01-09), the results page, and any future component can share the scraped profile + quota state. Build the `ApifyQuotaChip` dashboard component that renders "Escaneos restantes: X" or "Escaneos ilimitados" based on the quota state, and mount it on `/results`.

Purpose: Plan 01-09 used local component state for the scrape flow because `AppContext` hadn't been extended yet. This plan makes the state first-class in the app-wide context, wires the `/input` page to use context dispatch instead of local state (if needed), and adds the dashboard chip. Component test uses happy-dom (from Plan 01-01 vitest config).

Output: Extended AppContext + new ApifyQuotaChip component + component test + results page mounts the chip.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-apify-linkedin-integration/01-CONTEXT.md
@CLAUDE.md
@.claude/rules/components.md
@src/context/AppContext.tsx
@src/app/results/page.tsx
@src/lib/i18n/en.json
@src/lib/i18n/es.json
@src/lib/types/index.ts
@src/hooks/useLinkedinScrape.ts

<interfaces>
From Plan 01-05 types (`src/lib/types/index.ts`):
```
export type { HarvestProfile } from "@/lib/schemas/linkedin-profile";
export type ApifyQuotaState = { remaining: number | null; plan: "free" | "starter" | "recommended" };
export type ApifyScrapeError = ...;
export type LinkedinProfileSource = "paste" | "apify" | "pdf";
```

From Plan 01-09 hook (`src/hooks/useLinkedinScrape.ts`):
```
useLinkedinScrape() -> { status, profile, errorKey, errorCode, quotaState, triggerScrape, reset }
```

From Plan 01-01 i18n keys (already in en.json + es.json):
- `apify.dashboard.remaining` — "Scans remaining: {count}" / "Escaneos restantes: {count}"
- `apify.dashboard.unlimited` — "Unlimited scans" / "Escaneos ilimitados"
- `apify.dashboard.label` — "LinkedIn scans" / "Escaneos de LinkedIn"

From existing `src/context/AppContext.tsx`:
- 1171 lines, large interface block around lines 61-118 per research
- Executor reads the current interface and adds fields in the same style
- Provider wraps with useState for each field, matches existing convention
- `useAppContext` hook is likely already exported
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend AppContext with Apify state fields</name>
  <files>src/context/AppContext.tsx</files>
  <read_first>
    - /Users/isaacgbc/profile-score/src/context/AppContext.tsx lines 1-200 (FULL read of interface + provider state — find the context value type and provider useState block)
    - /Users/isaacgbc/profile-score/src/hooks/useLinkedinScrape.ts (from Plan 01-09 — the hook logic moves INTO context here)
    - /Users/isaacgbc/profile-score/.planning/phases/01-apify-linkedin-integration/01-RESEARCH.md lines 707-715 (exact state fields to add)
  </read_first>
  <action>
    Step 1. Find the context value interface (likely `AppContextValue` or similar). Add these fields:
    ```
    scrapedProfile: HarvestProfile | null;
    scrapeStatus: "idle" | "scraping" | "done" | "error";
    scrapeErrorKey: string | null;
    scrapeErrorCode: string | null;
    apifyQuota: ApifyQuotaState | null;
    triggerLinkedinScrape: (url: string) => Promise<void>;
    resetLinkedinScrape: () => void;
    ```

    Step 2. Import the types:
    ```
    import type { HarvestProfile, ApifyQuotaState } from "@/lib/types";
    ```

    Step 3. In the provider body, add matching `useState` calls:
    ```
    const [scrapedProfile, setScrapedProfile] = useState<HarvestProfile | null>(null);
    const [scrapeStatus, setScrapeStatus] = useState<"idle" | "scraping" | "done" | "error">("idle");
    const [scrapeErrorKey, setScrapeErrorKey] = useState<string | null>(null);
    const [scrapeErrorCode, setScrapeErrorCode] = useState<string | null>(null);
    const [apifyQuota, setApifyQuota] = useState<ApifyQuotaState | null>(null);
    ```

    Step 4. Define `triggerLinkedinScrape` and `resetLinkedinScrape` as `useCallback` that delegate to the same POST `/api/scrape-linkedin` call from Plan 01-09's hook. Copy the hook's fetch + error mapping logic INTO the context (or keep the hook and have the context call `useLinkedinScrape` internally — either pattern works; pick the one that produces the cleaner diff).

    Recommended: REPLACE the local `useState` in Plan 01-09's hook with a pass-through to context. The hook becomes:
    ```
    export function useLinkedinScrape() {
      const ctx = useAppContext();
      return {
        status: ctx.scrapeStatus,
        profile: ctx.scrapedProfile,
        errorKey: ctx.scrapeErrorKey,
        errorCode: ctx.scrapeErrorCode,
        quotaState: ctx.apifyQuota,
        triggerScrape: ctx.triggerLinkedinScrape,
        reset: ctx.resetLinkedinScrape,
      };
    }
    ```
    The hook's fetch logic moves to the context provider's `triggerLinkedinScrape` callback.

    Step 5. Add the new fields to the context value object returned by the provider. Preserve all existing fields — additive only.

    Step 6. Run `npm run build`. Confirm the input page from Plan 01-09 STILL compiles because the `useLinkedinScrape` hook API surface is unchanged.
  </action>
  <verify>
    <automated>grep -q "scrapedProfile" src/context/AppContext.tsx && grep -q "scrapeStatus" src/context/AppContext.tsx && grep -q "apifyQuota" src/context/AppContext.tsx && grep -q "triggerLinkedinScrape" src/context/AppContext.tsx && grep -q "resetLinkedinScrape" src/context/AppContext.tsx && grep -q "HarvestProfile\\|ApifyQuotaState" src/context/AppContext.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "scrapedProfile" src/context/AppContext.tsx` succeeds
    - `grep -q "scrapeStatus" src/context/AppContext.tsx` succeeds
    - `grep -q "scrapeErrorKey" src/context/AppContext.tsx` succeeds
    - `grep -q "apifyQuota" src/context/AppContext.tsx` succeeds
    - `grep -q "triggerLinkedinScrape" src/context/AppContext.tsx` succeeds
    - `grep -q "resetLinkedinScrape" src/context/AppContext.tsx` succeeds
    - `grep -q "HarvestProfile" src/context/AppContext.tsx` succeeds (type imported)
    - `grep -q "/api/scrape-linkedin" src/context/AppContext.tsx` succeeds (fetch call migrated into context)
    - Pre-existing context fields (journey, results, edits, generation — check a few via grep) are still present
    - `npm run build` exits 0
    - `src/hooks/useLinkedinScrape.ts` still works (may be a thin wrapper over context now, or unchanged — either is acceptable)
  </acceptance_criteria>
  <done>Context exposes 7 new fields, state management centralized, build clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create ApifyQuotaChip component + test</name>
  <files>src/components/results/ApifyQuotaChip.tsx, src/components/results/__tests__/ApifyQuotaChip.test.tsx</files>
  <read_first>
    - /Users/isaacgbc/profile-score/src/components/results (list files to match naming convention)
    - /Users/isaacgbc/profile-score/.planning/phases/01-apify-linkedin-integration/01-CONTEXT.md D-24
    - /Users/isaacgbc/profile-score/src/lib/i18n/en.json (confirm apify.dashboard.* keys exist from Plan 01-01)
    - /Users/isaacgbc/profile-score/src/context/I18nContext.tsx (confirm `useT` export)
  </read_first>
  <behavior>
    - Test 1: Renders "Scans remaining: 1" (EN) when `quota = { remaining: 1, plan: "free" }`
    - Test 2: Renders "Scans remaining: 0" when `quota = { remaining: 0, plan: "free" }`
    - Test 3: Renders "Unlimited scans" when `quota = { remaining: null, plan: "starter" }`
    - Test 4: Renders "Unlimited scans" when `quota = { remaining: null, plan: "recommended" }`
    - Test 5: Renders nothing (null) when `quota === null` (not yet loaded)
    - Test 6: ES locale: "Escaneos restantes: 1" renders when locale is "es"
    - Test 7: ES locale: "Escaneos ilimitados" renders for paid tier
  </behavior>
  <action>
    Step 1. Create `src/components/results/ApifyQuotaChip.tsx`:
    ```
    "use client";
    import { useT } from "@/context/I18nContext"; // or whatever the hook is actually named
    import type { ApifyQuotaState } from "@/lib/types";

    type Props = { quota: ApifyQuotaState | null };

    export function ApifyQuotaChip({ quota }: Props) {
      const { t } = useT();
      if (!quota) return null;

      const label = quota.remaining === null
        ? t("apify.dashboard.unlimited")
        : t("apify.dashboard.remaining").replace("{count}", String(quota.remaining));

      return (
        <span
          className="inline-flex items-center gap-2 px-3 py-1 rounded-full text-sm font-medium border"
          style={{
            backgroundColor: "var(--surface-2)",
            color: "var(--text-primary)",
            borderColor: "var(--border-subtle)",
          }}
          aria-label={t("apify.dashboard.label")}
          data-testid="apify-quota-chip"
        >
          {label}
        </span>
      );
    }
    ```

    Step 2. Create test file `src/components/results/__tests__/ApifyQuotaChip.test.tsx`:
    - Uses happy-dom environment (default from Plan 01-01 vitest config)
    - `import { render, screen } from "@testing-library/react"` — if not installed, install `@testing-library/react` + `@testing-library/jest-dom` as devDep and add to package.json (consider adding to Plan 01-01 retroactively; if too late, install here)
    - Actually — to minimize test tooling scope, use a simpler approach: test the component function directly by rendering it via `ReactDOM.render` or using the component's return value. OR install react testing library as needed.
    - Preferred: install `@testing-library/react@^16.1.0 @testing-library/jest-dom@^6.6.3` as devDep, wrap tests in a mock I18n provider that returns hardcoded translations
    - Mock `useT` to return `{t: (key: string) => <known translation>}` per test
    - Implement all 7 behaviors. For i18n tests, change the mock return based on test case.

    Step 3. If `@testing-library/react` is not already in devDeps, install it:
    ```
    npm install -D @testing-library/react@^16.1.0 @testing-library/jest-dom@^6.6.3 @testing-library/user-event@^14.5.2
    ```
    And add to `vitest.config.ts` setupFiles: `setupFiles: ["./vitest.setup.ts"]`, then create `vitest.setup.ts`:
    ```
    import "@testing-library/jest-dom/vitest";
    ```

    Step 4. Zero raw Tailwind colors. Component uses inline style with CSS variables OR arbitrary-value Tailwind classes `bg-[var(--surface-2)]`.
  </action>
  <verify>
    <automated>grep -q "export function ApifyQuotaChip" src/components/results/ApifyQuotaChip.tsx && grep -q "apify.dashboard.remaining" src/components/results/ApifyQuotaChip.tsx && grep -q "apify.dashboard.unlimited" src/components/results/ApifyQuotaChip.tsx && grep -q "var(--" src/components/results/ApifyQuotaChip.tsx && npx vitest run src/components/results/__tests__/ApifyQuotaChip.test.tsx --reporter=verbose && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/results/ApifyQuotaChip.tsx` exports `ApifyQuotaChip`
    - `grep -q "\"use client\"" src/components/results/ApifyQuotaChip.tsx` succeeds
    - `grep -q "apify.dashboard.remaining" src/components/results/ApifyQuotaChip.tsx` succeeds
    - `grep -q "apify.dashboard.unlimited" src/components/results/ApifyQuotaChip.tsx` succeeds
    - `grep -q "quota.remaining === null" src/components/results/ApifyQuotaChip.tsx` succeeds (paid tier check)
    - `grep -q "var(--" src/components/results/ApifyQuotaChip.tsx` succeeds (CSS variables)
    - `grep -E "bg-(red|blue|green|yellow|gray|slate)-[0-9]" src/components/results/ApifyQuotaChip.tsx` returns 0 matches
    - Test file exists
    - `npx vitest run src/components/results/__tests__/ApifyQuotaChip.test.tsx` shows 7 passing tests
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Component renders free/paid states in both locales, 7 component tests pass, build clean.</done>
</task>

<task type="auto">
  <name>Task 3: Mount ApifyQuotaChip on /results page</name>
  <files>src/app/results/page.tsx</files>
  <read_first>
    - /Users/isaacgbc/profile-score/src/app/results/page.tsx (FULL read — find the header/toolbar region where the chip should mount, near the existing plan/export toolbar)
    - /Users/isaacgbc/profile-score/.planning/phases/01-apify-linkedin-integration/01-CONTEXT.md D-24
  </read_first>
  <action>
    Step 1. Find the top-of-page header/toolbar region in `src/app/results/page.tsx` (where the audit title, plan badge, or export buttons render).

    Step 2. Add imports:
    ```
    import { ApifyQuotaChip } from "@/components/results/ApifyQuotaChip";
    import { useAppContext } from "@/context/AppContext";
    ```

    Step 3. Inside the component body, destructure `const { apifyQuota } = useAppContext();` and render `<ApifyQuotaChip quota={apifyQuota} />` in the header area. Place it near the existing plan/tier indicator if one exists, or at the top of the page controls.

    Step 4. If the page is a Server Component, the chip's `"use client"` directive is compatible (client components can be rendered from server components). If the parent needs `"use client"` to read context, confirm whether that's already the case — `AppContext` is client-only.

    Step 5. Do NOT restructure the results page layout. Additive only.
  </action>
  <verify>
    <automated>grep -q "ApifyQuotaChip" src/app/results/page.tsx && grep -q "apifyQuota\\|useAppContext" src/app/results/page.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "ApifyQuotaChip" src/app/results/page.tsx` succeeds
    - `grep -q "apifyQuota\\|useAppContext" src/app/results/page.tsx` succeeds
    - `npm run build` exits 0
    - Pre-existing results page sections (ScoreCardGrid, etc.) are still rendered
  </acceptance_criteria>
  <done>Chip mounted on /results, pulls from context, build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Context state -> component | Client state, read-only consumption |
| Component render -> DOM | React auto-escapes; no dangerouslySetInnerHTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 (T-08: quota bypass via client state tampering) | Elevation of Privilege | Context `apifyQuota` | mitigate | Client-side `apifyQuota` is UX only. Enforcement is server-side (Plan 01-07 route). A user tampering with local state cannot bypass the quota check because every scrape request goes through the server. Plan 01-07 tests 7-9 enforce server-side quota. |
| T-10-02 (Information disclosure via chip label to shared devices) | Information Disclosure | ApifyQuotaChip | accept | Showing "Scans remaining: 0" reveals the user has exhausted their quota — this is the intended UX. No PII disclosed. |
| T-10-03 (T-07: XSS via context-stored profile rendered in chip) | Tampering | ApifyQuotaChip | mitigate | Chip only renders `quota.remaining` (number) and `plan` (enum). Never renders scraped profile text. React escapes the number via JSX. |
| T-10-04 (CSS variable fallback missing) | UX degradation | Inline styles | accept | If CSS variables fail to load, React falls back to browser default colors. Component remains readable. Not a security concern. |
</threat_model>

<verification>
- [ ] `npx vitest run src/components/results/__tests__/ApifyQuotaChip.test.tsx` shows 7 passing tests
- [ ] `npm run build` exits 0
- [ ] AppContext exposes new fields without removing existing ones
- [ ] Chip renders on /results
- [ ] Zero raw Tailwind color classes in new files
</verification>

<success_criteria>
1. AppContext centralizes scrape state (scrapedProfile, scrapeStatus, scrapeErrorKey, scrapeErrorCode, apifyQuota, triggerLinkedinScrape, resetLinkedinScrape)
2. ApifyQuotaChip renders "Scans remaining: X" / "Unlimited scans" based on quota state
3. Both EN and ES copy render correctly
4. Chip mounts on /results page via context
5. CSS variables only (no raw Tailwind colors)
6. 7 component tests pass, build clean
</success_criteria>

<output>
After completion, create `.planning/phases/01-apify-linkedin-integration/01-10-SUMMARY.md` with:
- Whether @testing-library/react was installed here or already present
- Final test count for the component (7)
- Confirmation that existing AppContext fields were preserved (grep-verified)
</output>