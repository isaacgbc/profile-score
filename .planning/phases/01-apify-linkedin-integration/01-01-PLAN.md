---
phase: 01-apify-linkedin-integration
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - vitest.config.ts
  - src/lib/i18n/en.json
  - src/lib/i18n/es.json
  - src/lib/schemas/linkedin-profile.ts
  - src/lib/services/__tests__/fixtures/apify-profile-sample.json
  - src/lib/services/__tests__/fixtures/apify-profile-empty.json
  - src/lib/services/__tests__/fixtures/apify-profile-minimal.json
  - src/lib/schemas/__tests__/linkedin-profile.test.ts
  - src/lib/i18n/__tests__/apify-keys-parity.test.ts
  - .env.example
autonomous: false
requirements:
  - REQ-01
  - REQ-03
must_haves:
  truths:
    - "vitest installed and runnable via `npx vitest run`"
    - "Zod schema validates linkedin.com/in/... URLs and rejects all other hosts"
    - "All apify.* i18n keys exist in both en.json and es.json with exact parity"
    - "Fixture files representing the 3 Apify response shapes (normal, empty, minimal) exist"
  artifacts:
    - path: "vitest.config.ts"
      provides: "vitest config with happy-dom env + tsconfig paths alias"
    - path: "src/lib/schemas/linkedin-profile.ts"
      provides: "scrapeLinkedinRequestSchema + harvestProfileSchema + HarvestProfile type"
      contains: "export const harvestProfileSchema"
    - path: "src/lib/schemas/__tests__/linkedin-profile.test.ts"
      provides: "Schema round-trip tests"
    - path: "src/lib/i18n/__tests__/apify-keys-parity.test.ts"
      provides: "EN↔ES key parity enforcer"
  key_links:
    - from: "vitest.config.ts"
      to: "tsconfig.json"
      via: "path alias @/* resolution"
      pattern: "@/lib/schemas"
    - from: "src/lib/schemas/linkedin-profile.ts"
      to: "src/lib/i18n/en.json"
      via: "error message keys match apify.error.*"
      pattern: "apify.error.invalidUrl"

user_setup:
  - service: apify
    why: "LinkedIn profile scraping via HarvestAPI actor"
    env_vars:
      - name: APIFY_API_TOKEN
        source: "Apify Console → Settings → Integrations → Personal API tokens"
      - name: APIFY_FINGERPRINT_SALT
        source: "Generate random 32-byte hex string (for anonymous user fingerprint hashing)"
      - name: APIFY_ENABLED
        source: "Set to 'true' (emergency kill switch; set 'false' to disable scrape route without redeploy)"
    dashboard_config:
      - task: "Create Apify account with billing enabled"
        location: "https://console.apify.com/billing"
      - task: "Run ONE manual probe scrape against a known-public LinkedIn profile (e.g. https://linkedin.com/in/isaacgbc) via harvestapi/linkedin-profile-scraper actor UI"
        location: "https://console.apify.com/actors/harvestapi~linkedin-profile-scraper"
---

<objective>
Install vitest as the phase test runner, create the defensive Zod schemas for the Apify request and HarvestAPI response shapes, seed all user-facing i18n keys (EN+ES parity) for the phase, capture the 3 Apify response fixtures (normal, private/empty, minimal), and document the new env vars.

Purpose: Wave 0 unblocks every downstream task by giving every executor (a) a working test runner, (b) typed Zod contracts they can import, (c) i18n keys they can reference without adding new ones, and (d) fixture JSON that mocks Apify responses in unit tests. This is THE foundation wave.

Output: Working vitest config + `linkedin-profile.ts` Zod module + 3 fixture files + 12 new i18n keys in both locales + parity test that fails CI if keys drift.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-apify-linkedin-integration/01-CONTEXT.md
@.planning/phases/01-apify-linkedin-integration/01-RESEARCH.md
@.planning/phases/01-apify-linkedin-integration/01-VALIDATION.md
@CLAUDE.md
@CLAUDE.local.md
@src/lib/schemas
@src/lib/i18n/en.json
@src/lib/i18n/es.json
@package.json
@tsconfig.json

<interfaces>
<!-- Existing Zod schema conventions - executor should mirror these patterns -->

From src/lib/schemas/ (existing pattern):
- Schemas are plain named exports: `export const fooSchema = z.object({...})`
- Types inferred: `export type Foo = z.infer<typeof fooSchema>`
- Use `.passthrough()` for LLM/actor outputs that may drift
- Use `.safeParse()` at route boundaries, never `.parse()`

From src/lib/i18n/en.json (existing structure):
- Nested object namespaces: `t.progress.stageA`, `t.input.foo`, `t.bugReport.bar`
- Interpolation uses `{count}` curly placeholder
- EVERY key in en.json MUST exist in es.json with exact same path

From research §Zod schema (example 3):
```typescript
export const scrapeLinkedinRequestSchema = z.object({
  url: z.string().min(10).max(500).refine(
    (v) => {
      try { const u = new URL(v); return u.hostname.includes("linkedin.com") && u.pathname.includes("/in/"); }
      catch { return false; }
    },
    { message: "must be a linkedin.com/in/... URL" }
  ),
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install vitest + configure test runner</name>
  <files>package.json, vitest.config.ts</files>
  <read_first>
    - /Users/isaacgbc/profile-score/package.json (full — note existing scripts + devDeps)
    - /Users/isaacgbc/profile-score/tsconfig.json (full — extract path alias config)
    - .planning/phases/01-apify-linkedin-integration/01-VALIDATION.md §Test Infrastructure
  </read_first>
  <action>
    1. Run: `npm install -D vitest@^2.1.8 @vitest/ui@^2.1.8 happy-dom@^15.11.7 vite-tsconfig-paths@^5.1.4`
    2. Create `vitest.config.ts` at repo root with:
       ```ts
       import { defineConfig } from "vitest/config";
       import tsconfigPaths from "vite-tsconfig-paths";

       export default defineConfig({
         plugins: [tsconfigPaths()],
         test: {
           environment: "happy-dom",
           globals: true,
           include: ["src/**/*.test.{ts,tsx}"],
           exclude: ["node_modules", ".next", "src/lib/services/__tests__/linkedin-experience-archetype.test.ts"],
           setupFiles: [],
           testTimeout: 10_000,
         },
         resolve: {
           alias: { "@": "/src" },
         },
       });
       ```
    3. Add scripts to package.json (inside the existing `"scripts"` object, preserving all existing scripts):
       ```json
       "test": "vitest",
       "test:run": "vitest run",
       "test:ui": "vitest --ui"
       ```
    4. Exclude the existing tsx-script test file from vitest (it has its own tsx-script contract and should continue working under `npx tsx`). The `exclude` entry above handles this.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | grep -E "No test files found|passed|failed" && npx vitest --help > /dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --help` exits with status 0
    - `package.json` contains the exact strings `"test": "vitest"`, `"test:run": "vitest run"`, `"test:ui": "vitest --ui"`
    - `package.json` devDependencies contains `"vitest"`, `"@vitest/ui"`, `"happy-dom"`, `"vite-tsconfig-paths"`
    - `vitest.config.ts` exists at repo root
    - `grep -q "environment: \"happy-dom\"" vitest.config.ts` succeeds
    - `grep -q "tsconfigPaths" vitest.config.ts` succeeds
    - `npm run build` still exits 0 (zero TS errors introduced)
  </acceptance_criteria>
  <done>vitest runs (empty-file case is OK at this point), scripts added, build still clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Zod schemas for Apify request + HarvestAPI response</name>
  <files>src/lib/schemas/linkedin-profile.ts, src/lib/schemas/__tests__/linkedin-profile.test.ts</files>
  <read_first>
    - /Users/isaacgbc/profile-score/.planning/phases/01-apify-linkedin-integration/01-RESEARCH.md lines 522-580 (Example 3: Zod schema — full example)
    - /Users/isaacgbc/profile-score/src/lib/schemas/ (list files; mirror existing naming + export conventions)
    - /Users/isaacgbc/profile-score/src/lib/services/linkedin-parser.ts (skim LINKEDIN_SECTION_IDS + header regex to understand which section slots the formatter must emit)
  </read_first>
  <behavior>
    - Test 1: `scrapeLinkedinRequestSchema.safeParse({url: "https://linkedin.com/in/someuser"}).success` === true
    - Test 2: `scrapeLinkedinRequestSchema.safeParse({url: "https://linkedin.com/in/someuser/"}).success` === true (trailing slash allowed)
    - Test 3: `scrapeLinkedinRequestSchema.safeParse({url: "https://linkedin.com/in/user?trk=abc"}).success` === true (query allowed at schema level; normalization strips it later)
    - Test 4: `scrapeLinkedinRequestSchema.safeParse({url: "https://google.com/in/user"}).success` === false (non-linkedin host rejected)
    - Test 5: `scrapeLinkedinRequestSchema.safeParse({url: "data:text/html,<script>"}).success` === false (SSRF protection via data: URI)
    - Test 6: `scrapeLinkedinRequestSchema.safeParse({url: "javascript:alert(1)"}).success` === false
    - Test 7: `scrapeLinkedinRequestSchema.safeParse({url: "//linkedin.com/in/user"}).success` === false (protocol-relative rejected)
    - Test 8: `scrapeLinkedinRequestSchema.safeParse({url: "https://linkedin.com/company/foo"}).success` === false (non /in/ path rejected)
    - Test 9: `scrapeLinkedinRequestSchema.safeParse({url: "x".repeat(600)}).success` === false (over 500 char limit)
    - Test 10: `harvestProfileSchema.safeParse({headline: "Eng", about: "...", experience: []}).success` === true (minimal)
    - Test 11: `harvestProfileSchema.safeParse({}).success` === true (all fields optional — defensive drift tolerance)
    - Test 12: `harvestProfileSchema.parse({headline: "Eng", unknownField: "x"}).unknownField` preserved via passthrough
  </behavior>
  <action>
    1. Create `src/lib/schemas/linkedin-profile.ts` with TWO exports:
       - `scrapeLinkedinRequestSchema` — request body validator. Use the exact pattern from 01-RESEARCH.md §Example 3 lines 528-536, PLUS add explicit rejection of `data:`, `javascript:`, `//` protocol-relative URLs:
         ```ts
         url: z.string().min(10).max(500)
           .refine((v) => !v.startsWith("//") && !v.toLowerCase().startsWith("data:") && !v.toLowerCase().startsWith("javascript:"), {
             message: "apify.error.invalidUrl"
           })
           .refine((v) => {
             try {
               const u = new URL(v);
               if (u.protocol !== "https:" && u.protocol !== "http:") return false;
               return u.hostname.endsWith("linkedin.com") && u.pathname.startsWith("/in/");
             } catch { return false; }
           }, { message: "apify.error.invalidUrl" })
         ```
       - `harvestProfileSchema` — response body validator. Copy verbatim from 01-RESEARCH.md §Example 3 lines 540-577 (experienceEntrySchema + full harvestProfileSchema with `.passthrough()`).
       - `export type HarvestProfile = z.infer<typeof harvestProfileSchema>;`
       - `export type ScrapeLinkedinRequest = z.infer<typeof scrapeLinkedinRequestSchema>;`
    2. Create `src/lib/schemas/__tests__/linkedin-profile.test.ts` with vitest tests implementing ALL 12 behaviors listed above. Use `import { describe, it, expect } from "vitest"`.
    3. All Zod error messages on URL refines MUST use the i18n key string `"apify.error.invalidUrl"` (so the API route can map error.message → i18n key directly — no second lookup).
  </action>
  <verify>
    <automated>npx vitest run src/lib/schemas/__tests__/linkedin-profile.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - File `src/lib/schemas/linkedin-profile.ts` exports `scrapeLinkedinRequestSchema`, `harvestProfileSchema`, `HarvestProfile`, `ScrapeLinkedinRequest`
    - `grep -q "export const scrapeLinkedinRequestSchema" src/lib/schemas/linkedin-profile.ts` succeeds
    - `grep -q "export const harvestProfileSchema" src/lib/schemas/linkedin-profile.ts` succeeds
    - `grep -q "export type HarvestProfile" src/lib/schemas/linkedin-profile.ts` succeeds
    - `grep -q "apify.error.invalidUrl" src/lib/schemas/linkedin-profile.ts` succeeds (error message is i18n key)
    - `grep -q "passthrough()" src/lib/schemas/linkedin-profile.ts` succeeds (drift tolerance confirmed)
    - `npx vitest run src/lib/schemas/__tests__/linkedin-profile.test.ts` shows 12 passing tests
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Zod schemas created, 12 tests pass, build clean.</done>
</task>

<task type="auto">
  <name>Task 3: Add all 12 apify.* i18n keys to EN + ES with parity test</name>
  <files>src/lib/i18n/en.json, src/lib/i18n/es.json, src/lib/i18n/__tests__/apify-keys-parity.test.ts</files>
  <read_first>
    - /Users/isaacgbc/profile-score/src/lib/i18n/en.json (full — note top-level namespaces like `progress`, `input`, `bugReport`)
    - /Users/isaacgbc/profile-score/src/lib/i18n/es.json (full — confirm exact key parity convention)
    - /Users/isaacgbc/profile-score/.planning/phases/01-apify-linkedin-integration/01-RESEARCH.md lines 726-788 (exact EN + ES strings for all new keys)
  </read_first>
  <action>
    1. Add to `src/lib/i18n/en.json` the following keys (merge into existing namespaces where the namespace exists, create new `apify` namespace at the top level):
       - `input.linkedinUrlPrimaryLabel` = "Paste your LinkedIn profile URL"
       - `input.linkedinUrlHelp` = "We'll analyze your full profile in seconds"
       - `input.linkedinUrlPlaceholder` = "https://linkedin.com/in/yourname"
       - `input.useCvAlternativeTitle` = "Prefer to upload your CV instead?"
       - `progress.stageScraping` = "Analyzing your LinkedIn profile..."
       - `apify.error.private` = "This LinkedIn profile is private. Make it public or upload your CV."
       - `apify.error.invalidUrl` = "That doesn't look like a LinkedIn profile URL. Try https://linkedin.com/in/yourname"
       - `apify.error.rateLimit` = "We're getting lots of requests. Please try again in a minute."
       - `apify.error.downtime` = "LinkedIn scraping is temporarily unavailable. You can upload your CV instead."
       - `apify.error.quotaExceeded` = "You've used your free scan. Upgrade to unlimited scans for $5."
       - `apify.error.generic` = "We couldn't analyze that profile. Please try again."
       - `apify.error.disabled` = "LinkedIn scanning is temporarily unavailable."
       - `apify.dashboard.remaining` = "Scans remaining: {count}"
       - `apify.dashboard.unlimited` = "Unlimited scans"
       - `apify.dashboard.label` = "LinkedIn scans"
       - `apify.actions.retry` = "Try again"
       - `apify.actions.switchToCv` = "Upload CV instead"
       - `apify.actions.upgrade` = "Upgrade plan"
    2. Add the EXACT same key paths to `src/lib/i18n/es.json` with these translations:
       - `input.linkedinUrlPrimaryLabel` = "Pega la URL de tu perfil de LinkedIn"
       - `input.linkedinUrlHelp` = "Analizaremos tu perfil completo en segundos"
       - `input.linkedinUrlPlaceholder` = "https://linkedin.com/in/tunombre"
       - `input.useCvAlternativeTitle` = "¿Prefieres subir tu CV?"
       - `progress.stageScraping` = "Analizando tu perfil de LinkedIn..."
       - `apify.error.private` = "Este perfil de LinkedIn es privado. Hazlo público o sube tu CV."
       - `apify.error.invalidUrl` = "Esa URL no parece un perfil de LinkedIn. Prueba con https://linkedin.com/in/tunombre"
       - `apify.error.rateLimit` = "Estamos recibiendo muchas solicitudes. Inténtalo de nuevo en un minuto."
       - `apify.error.downtime` = "El análisis de LinkedIn no está disponible ahora. Puedes subir tu CV."
       - `apify.error.quotaExceeded` = "Has usado tu escaneo gratuito. Actualiza para obtener escaneos ilimitados por $5."
       - `apify.error.generic` = "No pudimos analizar ese perfil. Inténtalo de nuevo."
       - `apify.error.disabled` = "El análisis de LinkedIn no está disponible temporalmente."
       - `apify.dashboard.remaining` = "Escaneos restantes: {count}"
       - `apify.dashboard.unlimited` = "Escaneos ilimitados"
       - `apify.dashboard.label` = "Escaneos de LinkedIn"
       - `apify.actions.retry` = "Intentar de nuevo"
       - `apify.actions.switchToCv` = "Subir CV en su lugar"
       - `apify.actions.upgrade` = "Actualizar plan"
    3. Create `src/lib/i18n/__tests__/apify-keys-parity.test.ts`: walks both JSON files, flattens to dotted paths, asserts the sets are equal. Fails loudly listing which keys are missing on which side. Also explicitly asserts presence of all 12 `apify.*` keys listed above and the 5 `input.*`/`progress.*` additions.
    4. Do NOT remove any existing keys. Use `jq` or the Read+Write tools to merge — never overwrite the entire file.
  </action>
  <verify>
    <automated>npx vitest run src/lib/i18n/__tests__/apify-keys-parity.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "\"linkedinUrlPrimaryLabel\"" src/lib/i18n/en.json` succeeds
    - `grep -q "\"linkedinUrlPrimaryLabel\"" src/lib/i18n/es.json` succeeds
    - `grep -q "\"stageScraping\"" src/lib/i18n/en.json` succeeds
    - `grep -q "\"stageScraping\"" src/lib/i18n/es.json` succeeds
    - `grep -q "\"apify\"" src/lib/i18n/en.json` succeeds
    - `grep -q "\"apify\"" src/lib/i18n/es.json` succeeds
    - `grep -c "quotaExceeded" src/lib/i18n/en.json` returns 1
    - `grep -c "quotaExceeded" src/lib/i18n/es.json` returns 1
    - `grep -q "Analizando tu perfil de LinkedIn" src/lib/i18n/es.json` succeeds (exact brief-locked ES copy)
    - `grep -q "Analyzing your LinkedIn profile" src/lib/i18n/en.json` succeeds
    - `npx vitest run src/lib/i18n/__tests__/apify-keys-parity.test.ts` passes (EN=ES key sets identical)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>All 18 new keys added to both locales, parity test green, build clean.</done>
</task>

<task type="auto">
  <name>Task 4: Capture 3 Apify response fixtures + document env vars</name>
  <files>src/lib/services/__tests__/fixtures/apify-profile-sample.json, src/lib/services/__tests__/fixtures/apify-profile-empty.json, src/lib/services/__tests__/fixtures/apify-profile-minimal.json, .env.example</files>
  <read_first>
    - /Users/isaacgbc/profile-score/.planning/phases/01-apify-linkedin-integration/01-RESEARCH.md lines 808-818 (assumptions A1-A5), lines 1013-1045 (new/modified files), lines 860-866 (env var inventory)
    - /Users/isaacgbc/profile-score/.env.example (if exists — skim existing conventions; create minimal if absent)
  </read_first>
  <action>
    1. Create `src/lib/services/__tests__/fixtures/apify-profile-sample.json` with a representative HarvestAPI output. Structure based on CONTEXT §specifics + research A1-A5. The fixture MUST be a single-element array matching what `client.dataset(runId).listItems()` returns: `[{ publicIdentifier, linkedinUrl, firstName, lastName, headline, about, location, experience: [{title, company, dateRange, description, location}, ...], education: [...], skills: [{name, endorsementCount}, ...], topSkills: [...], certifications: [...], recommendations: [{text, author}, ...], projects: [...], publications: [], honors: [], volunteering: [], languages: [], connectionsCount: 500, followerCount: 1200 }]`.
       - Use at least 3 experience entries, 2 education entries, 5 skills, 2 certifications, 2 recommendations (with `text` field so formatter can emit "first 3" per D-18), 1 project.
       - Make it realistic enough that the formatter test can assert concrete output strings.
       - If Isaac has run the actual probe scrape (see user_setup), paste that output instead — real data is preferred. Otherwise use synthetic-but-realistic data and mark with a top-level `_source: "synthetic-placeholder"` key (will be ignored by passthrough Zod).
    2. Create `src/lib/services/__tests__/fixtures/apify-profile-empty.json` as `[]` (empty array — this is what Apify returns on private profile per assumption A3).
    3. Create `src/lib/services/__tests__/fixtures/apify-profile-minimal.json` as a single-element array with ONLY `headline` and one `experience` entry set — everything else omitted (tests formatter's optional-field handling).
    4. Create or update `.env.example` at repo root (or append if exists) with:
       ```
       # Apify LinkedIn Integration (Phase 01)
       APIFY_API_TOKEN=apify_api_xxxxxxxxxxxxxxxxxxxx
       APIFY_FINGERPRINT_SALT=change-me-to-32-byte-random-hex
       APIFY_ENABLED=true
       ```
    5. This is a `checkpoint:human-action` hybrid: if Isaac has NOT yet run the Wave 0 probe scrape, flag that the fixture is synthetic and the real fixture must replace it before Task 3 of Plan 01-04 (scraper client tests). Document this in a top-level comment inside `apify-profile-sample.json` via a `_note` key.
  </action>
  <verify>
    <automated>test -f src/lib/services/__tests__/fixtures/apify-profile-sample.json && test -f src/lib/services/__tests__/fixtures/apify-profile-empty.json && test -f src/lib/services/__tests__/fixtures/apify-profile-minimal.json && node -e "JSON.parse(require('fs').readFileSync('src/lib/services/__tests__/fixtures/apify-profile-sample.json'))" && node -e "const a=JSON.parse(require('fs').readFileSync('src/lib/services/__tests__/fixtures/apify-profile-sample.json'));if(!Array.isArray(a)||!a[0].headline||!a[0].experience)process.exit(1)" && grep -q "APIFY_API_TOKEN" .env.example</automated>
  </verify>
  <acceptance_criteria>
    - 3 fixture files exist under `src/lib/services/__tests__/fixtures/`
    - All 3 are valid JSON (parse without errors)
    - `apify-profile-sample.json` is an array with at least 1 element; element has `headline`, `about`, `experience` (array, length >= 1), `education`, `skills`, `certifications`, `recommendations`
    - `apify-profile-empty.json` parses to `[]`
    - `apify-profile-minimal.json` is an array with 1 element that has `headline` but no `about`
    - `.env.example` contains the string `APIFY_API_TOKEN`
    - `.env.example` contains the string `APIFY_FINGERPRINT_SALT`
    - `.env.example` contains the string `APIFY_ENABLED`
  </acceptance_criteria>
  <done>Fixtures committed, env vars documented. Any synthetic fixture flagged for later replacement with real probe data.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5: CHECKPOINT — Isaac provisions Apify account + runs probe scrape</name>
  <files>.env.local, src/lib/services/__tests__/fixtures/apify-profile-sample.json</files>
  <action>CHECKPOINT — human action required. Isaac must: (a) provision Apify account with billing, (b) generate APIFY_API_TOKEN, (c) add APIFY_API_TOKEN + APIFY_FINGERPRINT_SALT + APIFY_ENABLED=true to .env.local AND Vercel env vars (Production/Preview/Development), (d) run one real probe scrape via https://console.apify.com/actors/harvestapi~linkedin-profile-scraper against a known-public profile, (e) replace the synthetic placeholder in src/lib/services/__tests__/fixtures/apify-profile-sample.json with the real response JSON (single-element array), (f) note the actual actor input field name (profileUrls vs urls vs profileLinks) for Plan 01-04, (g) note sync run latency — if greater than 50s, flag async pattern for Plan 01-04. Full instructions in how-to-verify below.</action>
  <verify><automated>test -f .env.local && grep -q "APIFY_API_TOKEN" .env.local && node -e "const a=JSON.parse(require('fs').readFileSync('src/lib/services/__tests__/fixtures/apify-profile-sample.json'));if(!Array.isArray(a)||!a[0]||!a[0].headline)process.exit(1)"</automated></verify>
  <done>.env.local contains APIFY_API_TOKEN (non-placeholder), Vercel env vars set, real Apify probe JSON committed as apify-profile-sample.json (not synthetic), latency + field-name notes captured for Plan 01-04.</done>
  <what-built>Task 4 created fixture placeholders and env var documentation. This checkpoint requires real Apify credentials and one real probe scrape to de-risk assumptions A1-A5 from 01-RESEARCH.md.</what-built>
  <how-to-verify>
    1. **Create Apify account** at https://console.apify.com/sign-up (skip if exists)
    2. **Enable billing** at https://console.apify.com/billing — add payment method (cost: ~$0.004 per scrape, probe = ~$0.01)
    3. **Generate API token** at https://console.apify.com/account/integrations → copy the token
    4. **Add to `.env.local`** (NOT `.env.example`):
       ```
       APIFY_API_TOKEN=<your-real-token>
       APIFY_FINGERPRINT_SALT=<run: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))">
       APIFY_ENABLED=true
       ```
    5. **Add same vars to Vercel env** at https://vercel.com/isaacgbc/profile-score/settings/environment-variables (Production + Preview + Development scopes)
    6. **Run probe scrape**: navigate to https://console.apify.com/actors/harvestapi~linkedin-profile-scraper, click "Run", paste `https://linkedin.com/in/isaacgbc` (or any known-public profile), run sync, **download the output JSON**, paste it into `src/lib/services/__tests__/fixtures/apify-profile-sample.json` replacing the synthetic placeholder. Keep only 1 element of the array if the actor returned multiple.
    7. Note the actual input field name used by the actor UI (`profileUrls` vs `urls` vs `profileLinks`) — add a comment at top of `src/lib/services/apify-scraper-client.ts` in the next plan noting this.
    8. Measure and note the sync run latency (the probe's duration) — if over 50s, flag that async pattern may be needed in Plan 01-04.
  </how-to-verify>
  <resume-signal>Type "approved" after token is set, Vercel env is set, and real fixture replaces placeholder. Or type "blocked: <reason>" if Apify signup is delayed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | User-supplied LinkedIn URL enters via `/api/scrape-linkedin` (Zod validates at boundary) |
| Env → Runtime | `APIFY_API_TOKEN` stays server-only; Zod schemas read from server env |
| Source → Test fixtures | Test data is static JSON under `__tests__/fixtures/` — not runtime input, but must be parseable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 (T-09 variant: SSRF) | Information Disclosure | `scrapeLinkedinRequestSchema` | mitigate | Zod `.refine()` rejects: non-linkedin hosts, `data:` URIs, `javascript:` URIs, protocol-relative `//`, anything not starting with `https://...linkedin.com/in/`. Verified in Task 2 tests 4-8. |
| T-01-02 (T-07 variant: log injection) | Tampering | Zod error messages | mitigate | Error messages are static i18n keys (`apify.error.invalidUrl`), never user input. No format string interpolation of URLs into logError payload at schema layer. |
| T-01-03 (T-03 variant: API token exposure) | Information Disclosure | `.env.example` | mitigate | `.env.example` uses placeholder `apify_api_xxxxxxxxxxxxxxxxxxxx`; real token only in `.env.local` (gitignored) + Vercel env. NEVER prefixed `NEXT_PUBLIC_`. |
| T-01-04 (T-10 variant: cache poisoning via URL aliasing at schema level) | Tampering | URL validation | mitigate | Schema validation is the first gate; URL normalization in Plan 01-03 is the second. Both layered. Host check uses `hostname.endsWith("linkedin.com")` to correctly reject `evil.com/linkedin.com/in/...` style attacks. |
</threat_model>

<verification>
- [ ] `npx vitest run` runs without "No test files found" (tests exist)
- [ ] `npx vitest run src/lib/schemas src/lib/i18n` passes all tests
- [ ] `npm run build` exits 0 (TypeScript compilation clean)
- [ ] `grep -q "apify_api_" .env.example` succeeds (documentation present)
- [ ] Isaac has acknowledged the probe checkpoint and real fixture is committed OR synthetic placeholder is flagged for later replacement
</verification>

<success_criteria>
1. vitest installed and configured with happy-dom + tsconfig paths
2. `linkedin-profile.ts` Zod module exports both schemas and types
3. 12 schema tests pass (URL validation + response drift tolerance)
4. 18 new i18n keys added to BOTH en.json + es.json with exact parity
5. Parity test fails if any future key is added to one locale only
6. 3 fixture files exist and are valid JSON
7. `.env.example` documents `APIFY_API_TOKEN`, `APIFY_FINGERPRINT_SALT`, `APIFY_ENABLED`
8. `npm run build` is green
</success_criteria>

<output>
After completion, create `.planning/phases/01-apify-linkedin-integration/01-01-SUMMARY.md` with:
- Vitest version installed
- Confirmation of 12 Zod tests passing + parity test passing
- Fixture source (real probe vs synthetic placeholder)
- Any deviation from research assumptions A1-A5 discovered during probe
</output>